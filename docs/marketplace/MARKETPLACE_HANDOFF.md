# Marketplace Handoff

## Current Marketplace Objective

Использовать NimbaOS как базу для регулярной аналитики WB: продажи, остатки, реклама, карточки, отзывы, план/факт и рекомендации владельцу.

## Last Marketplace Session Summary

2026-09-01 duplicate-card correction: the owner confirmed that Nimba `парео синяя полоска` (`232092449:366203604`) and Galioni/canonical `туника синие волны` (`219179076:348718974`) are one physical product listed in different WB categories. The workflow alias, local config and live Sheet now use only `туника синие волны`; the accidental reference entry was cleared and the Nimba stock row retained its stable key with quantity 10. Summary combines 10 Nimba and 15 Galioni units on the canonical row. A fresh dry-run found 255 unchanged events, 53 unchanged stock rows, total WB stock 542 and no failed accounts.

2026-09-01 implementation/load: the second NimbaOS automation now exists locally and the authorized initial live-sheet load is complete through 2026-08-31. It projected 251 orders, three pre-handoff cancellation offsets and one explicit accepted-return movement across Nimba/Galioni; 250 rows were inserted and five existing stable-key rows were updated. Readback plus a fresh dry-run returned 255 unchanged events with zero inserts/updates and zero account failures. The owner-confirmed duplicate listing `парео леопард/пятна` is not a separate physical item: stable tuple `nimba:297175085:452136209` maps to `туника леопард/пятна`, and the accidental separate reference entry was cleared. The local workflow remains disabled; no production code deploy, production migration, worker, scheduler, WB API or marketplace mutation occurred.

2026-09-01 re-audit: the live FBS workbook baseline remains structurally correct, but the seven safeguards are not complete end to end because the daily FBS Sheet workflow does not exist in NimbaOS yet. Connector readback confirms `Europe/Moscow`, formula-driven `Сводка!C14 = 40`, 366 unique account/order rows, 376 unique event keys, zero missing cabinet/order/product/key/status/load fields, ten pre-shipment cancellation offsets, cancellation/accepted-return-aware formulas and 26 successful account/date control rows. Repository audit confirms that the automation registry, Prisma mapping, Server Actions and queue processor still support only `morning-wb-report`. The next development step is to implement retry-safe FBS event upsert/status updates, lifecycle event generation and post-load reconciliation before enabling a schedule. No source was mutated during this re-audit.

2026-09-01: upgraded the live Google Sheet `ФБС перемещение — автоматизированная версия` and replaced its manual FBS order quantities for 2026-07-28 through 2026-08-09 with the current production NimbaOS order set. The production DB now contains 366 unique account + externalOrderId orders for the window (165 WB Nimba / Grebnev and 201 WB Galioni / Snigireva; the source gained four late-fetched orders after the earlier 362-order audit). Every order was mapped to a spreadsheet product and written as one quantity-1 journal row with real WB order ID, `nmId`, `chrtId`, account, source, status, load timestamp and a unique idempotency key. Ten database-confirmed pre-shipment declines also received offsetting cancellation rows; 159 shipped client cancellations and four shipped defects were not restored to stock because actual return acceptance is not proven. Non-order history was preserved, the old operations sheet was copied to hidden `Операции_архив_до_БД_2026-09-01`, and API readback now matches production on every day and cabinet. The workbook was also prepared for daily automation: Europe/Moscow timezone, extended lifecycle operations, corrected `Сводка!C14`, open-ended latest-date formula, cancellation-aware stock formulas, hidden technical columns, warning-only calculation protection, and a readable `Контроль загрузки` tab with 26 successful date/account rows. Final checks show 366 order rows/quantity, 366 unique account-order IDs, 376 unique event keys including cancellations, zero missing technical order fields, and no formula errors. Physical stock total is now 1,414 after the corrected arrival and pre-shipment cancellations. No production DB, WB API, sync, WB, CRPT or marketplace state was changed. Visual browser QA could not be completed because Windows Computer Use stopped when it could not determine the current browser URL with enough confidence; connector-based structure, values, formulas, validations and formats were verified.

2026-09-01: reconciled the read-only Google Sheet `ФБС перемещение — автоматизированная версия` with production NimbaOS FBS orders for 2026-07-28 through 2026-08-09 by Moscow order-creation date. The sheet records 345 orders versus 362 unique production `fbs_orders`; no day matches in combined total. The largest breaks are 2026-08-07 (sheet 0 vs app 33) and 2026-08-08 (61 vs 26); the sheet's 2026-08-08 split is close to/partly equal to the app's combined 7-8 August totals, indicating a likely two-day manual lump. Production coverage is present for both cabinets, last fetched on 2026-08-31 Moscow time, and no duplicate account + externalOrderId pairs exist. The sheet is a useful append-only prototype but is not yet safe for an unattended daily workflow: it has no external order ID/idempotency key, no cancellation/return operations, uses product/operation display text as keys, has Europe/Amsterdam rather than Europe/Moscow timezone, and contains one overwritten summary formula (`Сводка!C14 = 41` while journal arrival is 40). No Google Sheet, production DB, sync, WB or CRPT state was changed.

2026-08-24: filled the live Google Sheet `OZON Unit 3.0` for both Ozon cabinets from the owner-supplied source pack. `UNIT 1 Nimba` now contains 56 current SKUs and `UNIT 1 Ozon Galioni` 51; all are FBO and use each cabinet's own Ozon product type. Prices, current 48% FBO commission and product volume came from the 2026-08-24 price/product exports; per-SKU buyout and modal shipment/delivery clusters use completed outcomes from 2026-05-26 through 2026-08-23; advertising is zero for Nimba and is allocated per delivered unit to the two Galioni SKUs with matching promotion charges. All Galioni costs matched the supplied cost workbook exactly; 14 Nimba articles matched exactly and 42 legacy aliases were mapped to the same model/material/color/size family. Internal per-unit costs, extra expenses, VAT and VAT recovery are zero; paid acceptance is `Нет`; Nimba tax is 8% and Galioni 7% with the workbook-compatible `УСН (с дохода)` label. Package dimensions were derived from Ozon volume using the existing 35 x 25 cm footprint so calculated volume remains unchanged. Three SKUs with only cancellations use their cabinet-level buyout to avoid division-by-zero. Existing formulas and workbook-wide settings were preserved; API readback matched every written manual value, all 24 formula anchors remain on each tab, no formula errors remain, and both tabs passed visual browser inspection. No Ozon-side price, card, advertising, stock or setting was changed.

2026-08-24: inspected the live Google Sheet `OZON Unit 3.0` and the current logged-in Ozon Seller interface without changing either source. The workbook has separate `UNIT 1 Nimba` and `UNIT 1 Ozon Galioni` tabs; one Ozon SKU is filled per row, cabinet categories must be mapped separately, and all unmarked columns are formula-driven. The minimum Ozon source pack per cabinet is: product export `Товары`, price export, FBO orders for the latest 90 completed days, and accrual-detail export for the same period; add the FBS orders report only when the cabinet actually sells from its own warehouse. Internal cost price, seller-paid logistics/packaging/fulfillment, tax/VAT settings and the paid-acceptance scenario still require owner confirmation. The hidden Ozon tariff table is dated 2026-07-15, so current commission values should be checked before treating the resulting unit economics as current. No Google Sheet cell, Ozon setting, report data, marketplace price, card, advertising state or local database row was modified.

2026-08-21: filled the 155-row `costPriceTemplate.xlsx` from the current mini-PC production cost-price reference and added 35 rubles to every source unit cost without changing the database. 85 rows matched the reference by seller article, one current cream article was linked through its historical `nmId`, and 69 alternate marketplace article names were explicitly mapped to the corresponding WB model/material/color/size family before applying the server value. The original two-column structure and formatting were preserved, all 155 cost cells are numeric and nonblank, and the final workbook was visually and structurally verified at `outputs/cost_price_fill_2026-08-21/costPriceTemplate_себестоимость_плюс_35.xlsx`. No WB API, sync, database write or WB-side action was performed.

2026-08-14: exported the validated WB Nimba margin-recovery HTML report to a layout-matched six-page A4 landscape PDF at `output/pdf/WB_Nimba_план_восстановления_маржи_01-12_августа_2026.pdf`. Added print-only CSS to preserve the report's colors, cards, chart, table and typography, removed browser file/date headers and footers, and kept the loss-chart heading with the chart. All six pages were rendered through Poppler and visually inspected; text extraction confirms all key sections and no local file footer. No analytical values or WB-side state changed.

2026-08-13: built an action plan for the negative WB Nimba margin on 1-12 August. Local `calculateReport` and bounded realization detail are fully covered; advertising coverage is also complete. Sales were 436,790.79 rubles, operating profit -17,149.00 rubles (-3.93%), commission 70,216.60 rubles (11.19% of the pre-platform-discount base), while advertising was only 0.49 ruble. A counterfactual using July's 0.97% effective commission produces about +46,966.23 rubles of operating profit (10.75% margin), confirming KVV/platform-discount conditions as the primary driver. Of 64 relevant article rows, 50 are negative; their losses total 38,795.66 rubles and the top 10 account for 57.08%. Recommended response: no blanket price rise; first limit 14 SKUs requiring more than 25% static-volume price uplift to break even, test a 7-10% seller-price/discount adjustment on the 17 negative SKUs whose break-even threshold is within 15%, protect the 11 already-profitable rows, and recalculate after three full days. The static cabinet thresholds are +5.6% for zero margin and +12.8% for about 5% margin, but they exclude demand elasticity and possible SPP changes. A self-contained, visually checked report is at `output/reports/nimba_margin_recovery_august_2026/nimba_margin_recovery_august_2026.html`. No price, discount, advertising, sync, DB, or WB-side mutation was performed.

2026-08-13: repaired delivery of the July/August WB Nimba commission diagnostic after its first embedded MCP artifact did not open. Recovered the complete canonical report, replaced width-heavy native tables with equivalent compact narrative summaries, changed the same-data daily grouped bars to a responsive two-series line chart, corrected currency display to rubles, and packaged a self-contained HTML report at `output/reports/commission_shift_august_2026/commission_shift_august_2026.html`. Canonical validation and packaging passed. The diagnostic browser loaded the report, but the strict verifier counted the Windows vertical-scrollbar width as horizontal overflow; final delivery therefore records structural-only browser QA. No data, calculation, sync or WB-side state changed.

2026-08-13: audited the negative WB Galioni commission for 1-30 April. The commission line is correctly negative at -1,420.92 rubles: sale rows contributed -2,016.15 rubles and return reversals reduced the seller benefit by 595.23 rubles. Weighted KVV was 34.50% versus a 34.99% platform discount, so the commission line increased the amount due to the seller. This is not the same as total profit. After all modeled costs, April operating profit was only 3,708.03 rubles at 1.21% margin. Coverage is complete, all 2,396 checked `rrdId` values are unique, and the report matches independent source-row aggregation to the kopeck. No WB API sync or WB-side mutation was performed.

2026-08-13: verified the WB Nimba commission increase for 1-12 July versus 1-12 August from the local database. Commission rose from 10,255.28 to 70,216.60 rubles (6.85x) even though sales fell 36.07% and net units fell 40.55%. The effective commission burden on the pre-platform-discount base rose from 0.97% to 11.19% because weighted KVV increased 7.66 percentage points while the platform discount decreased 4.73 points. Coverage is complete, all 8,362 checked `rrdId` values are unique, and `calculateReport` matches an independent raw-row aggregation to the kopeck. Galioni shows the same direction (5.71x commission growth despite lower sales), ruling out a Nimba-only calculation defect. Exact contractual attribution still requires the WB cabinet tariff history or Tariff Constructor options; no API sync or WB-side mutation was performed.

2026-08-13: audited late historical withdrawal queues in both cabinets. Nimba's new 28-row file was entirely canceled/defect orders; Galioni had 40 unconfirmed withdrawal tasks currently attached to canceled orders, including 11 rows in two old exports. All 68 unconfirmed tasks were canceled locally with audit events and their KIZ circulation restored to `IN_CIRCULATION`; physical state remains `RETURN_EXPECTED` until actual receipt. Future sync/export excludes this case, while already confirmed CRPT withdrawals remain unchanged and still require return-to-circulation after physical return. Final audit found zero open canceled-order withdrawals in Nimba and Galioni. The obsolete Nimba file and affected old Galioni files must not be uploaded unchanged.

2026-08-13: full FBS history is now operationally searchable without making pages heavy. Orders, KIZs, CRPT tasks, supplies and WB actions search/filter/sort the full account history on the server and return pages of 25/50/100 rows; one optional history calendar narrows all five journals. Analytics now places a compact article search beside total direct OP, margin, profitability and buyout percentage, with profitability/buyout also available per article. Profitability is OP divided by total expenses; buyout is net sales divided by sales plus cancellations, excluding unfinished orders. Encrypted KIZ values remain encrypted at rest and are never bulk-returned to the browser.

2026-08-13: FBS UI history was made bounded without deleting source data: the page loads an operationally prioritised window of up to 100 rows from each heavy journal, paginates visible results and shows exact all-history counts. Every table supports click-header ascending/descending sort. FBS analytics now uses the shared range calendar and shows article orders, cancellations, direct FBS OP and margin. Direct OP includes realization expenses, COGS and the account tax rate (currently 0% for the user's seller) but excludes shared advertising because no reliable FBS/FBO allocation exists. The requested sold-order KIZ was explicitly corrected locally to `WITHDRAWN` with an audit event after exact encrypted-code verification; the user remains responsible for completing the matching CRPT withdrawal. No WB/CRPT write or sync was performed.

2026-08-13: diagnosed the CRPT error `МОД по указанным ИНН ..., КПП null, ФИАС null не найдены` for FBS remote-sale withdrawal. The participant is an individual entrepreneur, so `КПП null` is expected; the blocking field is the missing FIAS ID / registered place of business. For light-industry remote sale, CRPT requires a MOD/ФИАС ID from 2026-03-01 even when the visible form does not mark the location field with `*`. The participant must add an active MOD under `Профиль -> МОД -> Легкая промышленность`, then select that place in a newly created withdrawal document. Documents processed with errors are not accounted for, so the same KIZs can be submitted again after correcting the header; no return-to-circulation action is needed for the failed documents.

2026-08-13: CRPT withdrawal files now include the required per-unit price. The value is the linked WB FBS order `convertedPrice` converted from hundredths to rubles and is not increased for VAT (seller VAT is 0%). Four prior withdrawal downloads were rebuilt as separate corrected files with 9, 16, 163 and 179 KIZ rows; all rows matched and originals were preserved. Read-only local evidence also resolved the apparent warehouse anomaly: the 57 `IN_STOCK + Статус в ЧЗ не указан` units (29 Galioni, 28 Nimba) all came from WB metadata on pickup-canceled/defect orders and had historical order links. They are not anonymous spare warehouse codes; after deployment, a normal bounded operational sync should let the repaired lifecycle logic reattach/reclassify them. No sync or data mutation was run in this session.

2026-08-12: FBS return semantics were corrected. WB statuses `canceled_by_client` (client cancellation at pickup) and `defect` are post-handoff returns, so the original KIZ remains tied to the order and no replacement code should be requested. `Статус в ЧЗ не указан` means only that NimbaOS received/imported the KIZ without a confirmed initial circulation state; it does not mean a new KIZ is needed. Managers/admins now see the same identification code used in the CRPT file, and every FBS section has search/status filters. In the CRPT form, fields without `*` are optional; after signing, record the actual document number/ID in NimbaOS rather than inventing a primary-document reference.

2026-08-12: reconciled the legacy Nimba KIZ export `fbs_kiz_operations_2026-08-12.xlsx` against local FBS data for 2026-07-28 - 2026-08-12. The file contains 163 unique remote-sale withdrawal tasks/orders. The apparent comparison `201 orders - 34 cancellations = 167` differs because six non-canceled orders had not reached WB handoff and therefore had no withdrawal task, one withdrawal task was already confirmed, and three orders already present in the export were canceled after handoff and still require the post-handoff withdrawal/return lifecycle. Converted the legacy file to the current one-column CRPT upload structure without changing application data.

2026-08-12: `/fbs` now supports a mass file workflow for Chestny Znak. The manager exports withdrawal and return-to-circulation codes as separate XLSX files, processes the appropriate document in CRPT, then confirms the whole exported batch in NimbaOS using one document number/date. The local KIZ circulation state is event-based and audited, but it is not a live CRPT status lookup; download alone does not mark a KIZ withdrawn or returned.

2026-08-12: fixed and verified FBS analytics from the local DB. WB Finance uses `deliveryMethod=FBS` primarily on zero-quantity logistics rows, so sale/return rows must be associated by financial `orderId` with `fbs_orders.externalOrderId`. For 2026-08-01 - 2026-08-11 the corrected results are: Nimba 161 FBS orders / 26 cancellations / 52 buyouts / 0 returns / 73,432.86 rub revenue / 52,763.15 rub transfer; Galioni 199 / 34 / 33 / 0 / 43,925.11 / 30,201.17. Official light-industry distance-sale guidance requires withdrawal after shipment, within 3 business days and no later than actual delivery, so the KIZ queue correctly starts at WB handoff rather than final buyout. Returns must be returned to circulation (or remarked if the marking is damaged/missing) before resale.

2026-07-25: created validated Excel `outputs/wb_supply_2026-07-25/Рекомендации_по_поставке_август-сентябрь_2026.xlsx` for `WB Nimba (WB_1)` and `WB Galioni (WB_2)`. Local DB only; no WB API/sync/WB-side changes. Orders and financial rows cover 2026-06-15 - 2026-07-19; stocks are from 2026-07-25 Moscow snapshots. The analysis treats Электросталь, Краснодар, Невинномысск, СПБ/СЦ Шушары and Котовск as zero stock and excludes in-way quantities. Nimba: 2,523 orders / 837 cancellations / 896 included stock / 1,891 excluded stock / 1,488 recommended supply units. Galioni: 2,544 / 834 / 416 / 836 / 1,726. Forecast applies a moderate post-peak decline: August 75% and September 45% of the blended demand base. Only Nimba child products are split as `article + size`.

2026-07-21: created validated Excel `outputs/wb_analytics_2026-07-21/WB_аналитика_01.05.2026-19.07.2026.xlsx` for `WB Nimba (WB_1)` and `WB Galioni (WB_2)`. Local DB only; no WB API/sync/WB-side changes. `REPORTS_PERIOD`, `wb_orders` and financial data cover 2026-05-01 - 2026-07-19; stocks are from 2026-07-21 snapshots. Full-period results: Nimba 3,566 orders / 1,980 net sold units / 2,730,399.55 rub sales / 234,218.42 rub OP / 8.6% margin / 1,200 sellable units; Galioni 3,650 orders / 2,059 net sold units / 2,616,749.06 rub sales / 74,489.45 rub OP / 2.9% margin / 528 sellable units. July daily pace shifted to Nimba: 52.79 net units/day vs Galioni 50.42, while second-half June was led by Galioni at 32.73 vs 24.60. Nimba child products are split only as `article + size`; all other products remain article-level. In-way quantities are excluded from stock.

2026-06-28: updated live Google Sheet `Утренний отчет WB` for both `WB Nimba` and `WB Galioni` from local DB only. Removed `ROMI`; added `Выкупили, шт` after `Выкупили, руб`; added `ЧП на 1 ед` after `ЧП`; added `Итого с начала года`. Daily June rows are filled through 2026-06-27 after verification; YTD totals cover 2026-01-01 - 2026-06-27. Plan/fact/progress rows and charts shifted down. Workflow guard was fixed so full-year reports are required for YTD, while ad coverage is checked for the current month window.

2026-06-27: diagnosed `WB Nimba (WB_1)` cancellations/product losses for 2026-06-22 - 2026-06-25 and created Excel `output/excel/wb_nimba_cancellations_2026-06-22_2026-06-25.xlsx`. Local DB only; no WB API/sync/WB-side changes. Added reusable script `scripts/analyze-nimba-cancellations.ts`. Key findings: Nimba cancellation pressure concentrated in `Парео` (60 cancellation rows in problematic subset, negative OP -2,216.08 rub) and several tunic size rows; worst ad loss was `туника с поясом леопард пятна` with 2,017.93 rub spend, 9,793 views / 410 clicks, 1 ad order, 0 sales and OP -2,473.41. Next safe step: restrict/stop poor ad rows and move budget only after checking stock and confirming action.

2026-06-26: created Excel `output/excel/wb_cabinet_factor_analysis_2026-06-22_2026-06-25.xlsx` comparing `WB Galioni (WB_2)` and `WB Nimba (WB_1)` across finance, products, categories, ads, stocks, reviews/questions and coverage. Used local DB only via `calculateReport` with persisted ad stats plus bounded Prisma reads; no WB API/sync/WB-side changes. Coverage: `REPORTS_PERIOD` and `ADVERTISING_STATS` covered for both cabinets; latest stock snapshots from 2026-06-26; `wb_sales` and `wb_funnel_stats` had no rows for the period, so sales/funnel conclusions rely on financial reports, orders and ad stats. Main totals: Galioni sales 163,996.39 rub / OP 11,148.30 rub / margin 6.80% / buyout 59.91% / ad spend 2,747.02 rub; Nimba sales 112,431.20 rub / OP 751.48 rub / margin 0.67% / buyout 40.22% / ad spend 2,892.56 rub. Key drivers: Galioni sold 1.81x more units with better buyout and much lower logistics/storage/commission load; Nimba ads spent slightly more but produced fewer ad orders and much weaker order sum.

2026-06-19: created Excel `output/excel/wb_article_profitability_2026-06-01_2026-06-14.xlsx` for both WB cabinets from the preserved formatted workbook structure. Local DB only, no WB API/sync/code/DB changes. Replaced `Sold before returns` with total stock, kept the key profitability tables, no-sale sheet, and added two comparison sheets: user-style comparison from the provided loss-items workbook and Codex logical comparison of WB Nimba zero/negative margin rows against WB Galioni analogs. Totals: Galioni sales 348,118.03 rub / OP 32,074.11 rub / 45 sold rows / 23 no-sale rows; Nimba sales 421,837.80 rub / OP 53,037.91 rub / 56 sold rows / 77 no-sale rows. Workbook checks: no `???` factor text and no old sold-before-returns header.

2026-06-19: regenerated Excel `output/excel/wb_article_profitability_2026-01-01_2026-06-17.xlsx` after dated article versions were implemented. Local DB only, no WB API/sync. Current totals: Galioni sales 2,157,583.34 rub / OP 185,410.06 rub / 56 sold rows / 11 no-sale rows; Nimba sales 2,913,759.88 rub / OP 398,842.06 rub / 72 sold rows / 60 no-sale rows. `???` factor text was checked and not present.

2026-06-18: created Excel report `output/excel/wb_article_profitability_2026-01-01_2026-06-17.xlsx` for both active WB cabinets, with separate sheets for `WB Galioni (WB_2)` and `WB Nimba (WB_1)`, plus `Сводка` and `Не продавались`. Used local DB only through `calculateReport`; no WB API, sync or DB/code changes. `REPORTS_PERIOD` was fully covered for 2026-01-01 - 2026-06-17. `ADVERTISING_STATS` did not cover the full January-June period, so ad/DRR influence is based only on available local ad stats. Main totals: Galioni sales 2,157,583.34 rub / OP 283,987.06 rub / 53 sold rows / 11 no-sale rows; Nimba sales 2,913,759.88 rub / OP 398,842.06 rub / 70 sold rows / 61 no-sale rows. For Nimba, size products are shown as `article + size`.

2026-06-13: created and revised PDF report `output/pdf/wb_nimba_report_2026-06-01_2026-06-12.pdf` for `WB Nimba (WB_1)` covering 2026-06-01 - 2026-06-12. Used local DB only through `getMorningReportData`; `REPORTS_PERIOD` and `ADVERTISING_STATS` were covered, latest stock snapshot was 2026-06-13 08:52 UTC, `SALES_PLAN_PERIOD` was not covered so plan/fact was excluded. Main findings: sales 352,246.94 rub / 202 units, ordered 648,048.13 rub, buyout 56.74%, operating profit 43,146.16 rub, margin 12.25%, ad spend 7,998.22 rub, DRR 2.27%; stock 3,755 units / 2,481,295 rub cost value, average turnover 624.4 days, 39 overstock SKU. User-requested revision: child products are shown as separate `article + size` rows; stock-risk table is only `Парео` and `Туники`, excluding stopped articles (`парео сирень2`, `парео фуксия`, listed long pareo, `пижама003`, `пижама004`).

2026-06-07: clarified and fixed stock risk interpretation in NimbaOS. `Оборачиваемость, дн.` is not a WB-provided field; it is calculated as current sellable stock divided by average daily non-return sales over the 30 completed days before the latest stock snapshot. `/stocks` now uses local `wb_sales` plus `realization_reports` sale quantities as fallback, and risk states are: `Нет остатка` when total sellable stock is zero; `Низкий остаток` up to 14 days; `В норме` 14-120 days; `Излишек` above 120 days with at least 10 units; `Нет продаж` when positive stock has no recent demand. Category filtering supports selecting multiple categories.

2026-06-01: created Google Sheet `Анализ детских парео и туник — май 2026` for child pareo/tunics supply planning. Used local DB only: `realization_reports` covered 2026-05-01 - 2026-05-31; latest stock snapshots were synced 2026-06-01. Main finding: Galioni child pareo stock is excessive versus May demand; Nimba child tunics need targeted replenishment in large sizes, especially white 134-152/152-164 and pink 134-152/152-164.

2026-05-25: filled Google Sheet `Утренний отчет WB`, tab `WB Galioni`, for 2026-05-01 - 2026-05-24 from local DB-backed report data. 2026-05-25 was left blank because the day was not covered yet; no ad-hoc WB API call or historical resync was performed.

2026-05-25: corrected `Заказано руб.` definition. It now includes all WB orders, including cancelled rows, so the metric reflects ordered ruble volume rather than non-cancelled/fulfilled order volume.

2026-05-25: fixed the ordered-rubles sync issue for morning-report prep. Report sync now refreshes the `wb_orders` source for the selected period.

2026-05-25: подготовлены метрики для будущего `Утренний отчет WB` по Galioni. `ДРР` уже был в фин. отчете; добавлены `Заказано руб.`, `ROMI %`, `Оборачиваемость, дн.` и локальный источник `getMorningReportData`. Google Sheet не заполнялся.

## Current Safe Next Step

For `OZON Unit 3.0`, review the completed profitability outputs before making any marketplace decision. Refresh the manual inputs only when the current price/commission export, cost workbook or rolling 90-day order/accrual window materially changes. Keep cabinet-specific category mappings. If exact physical package sides become available, replace the derived height x 35 x 25 cm values without changing Ozon-reported volume unless Ozon itself reports a new volume. Any Ozon price, promotion, card, advertising or stock action requires explicit owner confirmation.

For WB Nimba margin recovery, first review the report and approve the article cohorts before any WB-side action. The recommended first experiment is a 7-10% seller-price increase / seller-discount reduction only for the 17 negative articles with a static break-even threshold up to 15%, followed by a fresh three-completed-day profitability check. Temporarily limit the 14 articles requiring more than 25% uplift unless they are intentionally being liquidated. Preserve profitable child tunics and other rows already above 5% margin. Before action, also inspect current category tariff, Tariff Constructor options and seller level in WB. Any price, promotion, card, advertising or stock action requires explicit owner confirmation.

## Active Marketplace Risks

- The hidden tariff reference in `OZON Unit 3.0` shows an update date of 2026-07-15. The 107 populated rows currently use the 48% FBO commission from the 2026-08-24 price exports as a manual override; refresh it when Ozon changes the category tariff.
- Ozon products that are physically identical across Nimba and Galioni can have different marketplace categories; category and commission mapping must remain cabinet-specific.
- Ozon order exports can include buyer data. For unit-economics work, export analytical data with buyer-data and jewelry-barcode options disabled unless separately required.
- Нельзя менять цены, карточки, рекламу, ставки или остатки без подтверждения.
- Данные могут быть устаревшими или неполными; всегда проверять coverage.
- Для Galioni orders/sales могут отставать от financial reports; `Заказано руб.` зависит от `wb_orders`.
- Stock turnover and risk depend on local 30-day demand coverage; if recent `wb_sales` and `realization_reports` are stale, interpret `Нет продаж` cautiously.
- Рекомендации должны быть осторожными, особенно при низком объеме данных.
- FBS operational accounting is available under `/fbs`, and its migration is applied to the local development database only. Read-only WB contracts were smoke-tested for one local cabinet: 16 FBS positions and 215 WB stock units are now visible after checking the full local product-size catalog. No production migration, historical backfill or WB write was run. Local physical stock is authoritative; WB stock is a separate reconciliation value until an explicit publication.
- Marked FBS units must be tracked individually. Cancellation before handoff releases the KIZ; post-handoff returns stay quarantined until inspection and, when needed, return-to-circulation confirmation.
- Current KIZ intake supports a full DataMatrix code manually or via XLSX (`КИЗ` plus `chrtId` or `barcode`); direct PDF decoding is not implemented. A secure PDF importer needs a representative PDF/template and must decode locally without exposing full codes.
- If fulfillment scans/attaches the KIZ in WB while packing, WB FBS metadata provides the exact `orderId -> sgtin[]` mapping. This was live-verified for 20 of 24 latest orders across both local cabinets. NimbaOS should pull this mapping securely; a separate fulfillment screen is needed only if fulfillment does not record the code in WB.
- NimbaOS now pulls this mapping securely. Live execution created/assigned 20 encrypted KIZ units and 20 remote-sale withdrawal tasks with no reject, conflict or GTIN mismatch; an immediate rerun was fully idempotent.
- Nine additional Nimba KIZ mappings for 2026-07-28 were repaired after replacing stale workers. All 9 now show `Метаданные WB: Получены`; the former `Заблокировано` state was a UI conflation with Chestny Znak circulation, not a WB metadata failure.
- PDF labels alone are still insufficient when fulfillment only sticks the label physically and never scans it into WB. PDF import remains useful for knowing the pre-packing KIZ pool, but is not required for post-packing order mapping when WB metadata is populated.
- For marked FBS, the seller remains responsible for Chestny Znak documents. Before handoff cancellation only releases the assignment; after withdrawal and physical return, use return to circulation, while damaged/missing marking requires the applicable remarking path.
- FBS finance cannot be identified by filtering each sale row on `deliveryMethod=FBS`: WB often sets that value only on the related logistics row. Use the operational FBS order ID link and verify both FBS operational and Finance coverage.

## Read Next If Needed

- `docs/marketplace/ANALYTICS_PLAYBOOK.md`
- `docs/marketplace/REPORTS_GUIDE.md`
- `docs/marketplace/KPI_DEFINITIONS.md`
- `docs/marketplace/DAILY_CHECKLIST.md`
- `docs/marketplace/DECISION_RULES.md`
- `docs/core/DATABASE_ACCESS_GUIDE.md`

## Do Not Do

- Не вызывать WB API ad-hoc для отчета.
- Не делать full historical sync без подтверждения.
- Не выдавать опасную рекомендацию как автоматическое действие.

## Last Updated

2026-09-01 — reconciled the FBS movement Google Sheet against production daily FBS orders and recorded the workflow-readiness blockers; all source systems remained read-only.

2026-08-24 — filled and verified `OZON Unit 3.0` for 56 Nimba and 51 Galioni SKUs from the supplied Ozon and cost files; formulas/settings were preserved and no Ozon-side state was changed.

2026-08-24 — inspected `OZON Unit 3.0`, mapped every manual field to its source and verified the minimum Ozon Seller export paths for both cabinets; no source was changed.

2026-08-12 — fixed and DB-verified FBS analytics, added the requested six metrics, and confirmed that remote-sale KIZ withdrawal remains tied to shipment rather than buyout.

2026-07-25 — created and validated the two-cabinet August-September supply workbook with five warehouse exclusions and Nimba child size-level rows.

2026-07-21 — created and validated the two-cabinet sales/stock/supply Excel for 2026-05-01 - 2026-07-19; logged freshness, size-level Nimba logic and current supply risks.

2026-06-28 — updated `Утренний отчет WB` layout/data through 2026-06-26 and logged findings.

2026-06-28 - updated Google Sheet `WB Unit 3.0` tabs `UNIT WB Nimba (WB_1)` and `UNIT WB Galioni (WB_2)`: refreshed local product/price cache via the standard product sync, updated base price/discount/SPP, added DB products with costPrice > 50 RUB, and refreshed current cost prices. No WB-side price/card changes were made.
2026-08-13: delivered corrected Galioni historical withdrawal workbooks: 11 KIZs from the 2026-07-30 batch and 173 from the 2026-08-12 batch. Eleven canceled-order KIZs were excluded; all 184 remaining unique rows are still marked `WITHDRAWAL_REQUIRED`, and the previously verified per-unit prices were preserved. Use the corrected copies instead of the original 16/179-row files.
## FBS WB stock reconciliation (2026-09-01)

- `Остаток WB` in the live FBS accounting Sheet is now the WB-confirmed sellable FBS stock: Nimba 264, Galioni 278, total 542 at the verified snapshot.
- The local physical ledger is intentionally separate. If it is below WB, Summary shows `ВНЕСТИ ПОПОЛНЕНИЕ +N`; this is a warning and does not invalidate the WB snapshot load.
- Current live reconciliation has 11 missing-local-replenishment warnings. Positive local-minus-WB values are free/not-published stock and are not treated as missing replenishments.
- Unknown/ambiguous stock tuples are never guessed; they stop the load until a stable mapping is recorded.
