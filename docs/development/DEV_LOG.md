# Development Log

Canonical post-cleanup development history. Earlier entries are preserved in `docs/archive/development/DEV_LOG_2026-05_to_2026-09-02.md`.

## 2026-09-07 - FBS nomenclature discovery and confirmed physical-product mapping (BUG-037)

- Read-only production diagnosis verified host/runtime `fdb3f5e`, healthy services and successful FBS syncs. The first affected `FBS_MOVEMENT_SHEET` run was 2026-09-06 23:30 Moscow (`30a4ca8f-bfd2-432f-8e1e-0ef9def3520b`): nine tuples failed `resolveFbsProductName` while preparing WB stock snapshots. These were already present in `fbs_assortment_items` since 18:00, linked to products/sizes/barcodes, with `onHand=reserved=0` and 86 WB units. Independent inspection found no orders, movements or KIZ for these nine positions. The failure occurred before Sheet writes; missing local stock rows were not the incident cause.
- The owner confirmed nine cabinet tuples represent five physical products: blue swirls, black swirls, light green, green snake/leaf and leopard/spots, all with a belt. Exact names and the nine account/nmId/chrtId links are canonical in `src/lib/automations/fbs-product-aliases.ts`; the two cabinets retain separate WB snapshot keys. The new groups are distinct from existing similarly named products without a confirmed belt identity. Runtime defaults also apply to already-saved workflow settings; configured mappings retain priority.
- Added strict nomenclature planning: explicit approved aliases can register new names; existing exact Sheet tuple identities remain valid. An unknown new size/cabinet tuple requires confirmation even when its seller article resembles an existing name. Barcodes never identify a physical group on their own.
- Added a visible mapping review to `/automations/fbs-movement-sheet`: suggestions explain similarities, existing reference groups are selectable, and a manager can confirm a group or enter a new exact name. Suggestions do not write. Confirmation validates active cabinet/assortment and current Sheet identities, saves only the alias, refuses to rename confirmed tuples and protects concurrent settings/mapping edits, including destination changes and simultaneous cabinet discovery. A Sheet-read failure is shown as a warning rather than a false list of unmapped legacy products.
- Verified the live template read-only: reference products occupy `A4:A153` (47 populated), Summary formulas occupy `A8:T157`, and the reference/Summary footers lie outside those ranges. Approved names fill first empty reference cells as literal strings, with paired formula validation, capacity checks and readback before stock/operation writes. No initial stock or synthetic movement is created. Missing products in Summary now fail verification instead of being silently omitted. The mapping audit also excludes the reference footer.
- Strengthened `FBS_STOCKS_CURRENT` to refresh cards/size identities before querying stocks, without price API calls or material replacement. Existing size/barcode pairs retain `ProductSize.id`; stock sync selects a stable barcode and order resolution uses exact account/nmId/chrtId. Creation uses zero local balances; updates preserve existing balances, disabled and marking settings. Malformed responses or catalog failures cannot become fresh zero stocks. Cards-only refresh preserves stored prices even when Content returns a stale nonzero price.
- Verification covers nine listings to five names, a retry after reference/main writes but before control, no duplicated snapshots/events or synthetic stock movements, malformed API responses, stable catalog links and manager confirmation conflicts. The real workflow is exercised with mocked DB/Sheets using an old saved configuration; it plans five names and nine snapshots totaling 86 WB units, while a future unapproved leopard listing stops before any external write. Browser checks on the actual review component with isolated mock actions passed at desktop/mobile widths, for confirmation into existing/new groups and disabled VIEWER controls.
- During implementation and local verification, production, Google Sheets, WB, schema, migrations, environment and schedules were not changed. The subsequent owner-authorized rollout and Sheet execution are recorded below. A normal run through 2026-09-06 reads from the configured start date and does not require replaying earlier dates individually or historical WB resynchronization.

### Follow-up: two operational fulfillment indicators

- The owner approved two basic operational indicators without waiting for billing tariffs: system transfer to delivery and a confirmed minimum of WB acceptance. The current `Всего заказов` remains unchanged. Charges, repeated handling and return-processing tariffs are outside this change.
- WB documents one assembly task/order ID as one item. `supply.closedAt` dates the system close/transfer that changes tasks to `complete`; it does not prove physical handoff/acceptance of every unit. Unscanned tasks can require reshipment after a supply is closed. Supply-level `scanDt` likewise is not per-item acceptance. Local `shippedAt` and event `observedAt` are observation times and cannot silently substitute for physical event dates. Source: [WB FBS API](https://dev.wildberries.ru/openapi/orders-fbs/).
- Connected the pure `summarizeFbsDeliveryHandoffs` helper through a bounded local-DB service and the Sheet workflow. It explicitly reports `WB_SYSTEM_DELIVERY` / `WB_SUPPLY_CLOSED_AT`, deduplicates account/order IDs, retains later returns, detects multiple supply links and returns `units=null` with diagnostics when dates/links are incomplete. The service checks existing Sheet keys against local orders and refuses to silently truncate its 50,000-order bound. Its exactness applies to covered system-event evidence, not billed operations.
- Individual acceptance uses only `sorted`, `ready_for_pickup` and `sold` evidence from the same handoff cohort, with `observedAt <=` the end of the target Moscow day; the current status uses its `fetchedAt`. Later return/cancellation does not erase prior evidence. The display is always `не менее N` because incomplete historical statuses cannot establish total physical acceptance. Carrier statuses are excluded. No physical acceptance timestamp is invented. Source for WB status semantics: [WB sandbox status descriptions](https://dev.wildberries.ru/docs/openapi-other/sandbox-environment).
- Read-only production inspection found 652 orders, 635 with `shipmentApplied`, all 635 linked to `supply.closedAt`; 258 of these had late cancellation/defect statuses and must not be removed from past-transfer counts. Through 2026-09-05, system transfers total 630 across available history since July 28, versus 285 for August 10–September 5; the latter includes 8 orders created before August 10 and 101 later cancellations/defects. These are diagnostic counts, not verified fulfillment charges.
- The existing Sheet `Всего заказов` is 646 through September 5 and sums all retained operations since July 28, including 362 orders before `config.startDate=2026-08-10`. Both new indicators therefore use the earliest retained local/Sheet order date through the report target, with an explicit `28.07.2026–<target>` period in this dataset. They do not reuse the incremental sync start date.
- `Сводка!K4:L6` was verified empty and unmerged, with K/L widths 111/190px and row 4/5 heights 20/30px. The new local writer places full titles in K4/L4, values in K5/L5 and `Период, МСК` plus dates in K6/L6. It uses no merge, preserves the existing row-6 bottom border, and raises row 4 to 60px for wrapping. Occupied content/formulas/merges stop the workflow before writes; a successful metric readback is required before advancing the report date. No live cells, formats or formulas were changed during development.
- Independent current-history inspection found 517 orders with individual acceptance evidence among 635 system transfers through September 6; 118 late-return orders lacked prior acceptance evidence. These are diagnostic counts from the current evidence, not promises of historical `asOf` results: the report additionally filters observations by the target cutoff.
- Final automated checks passed: 153 tests, TypeScript type-check, full optimized Next.js build (29 generated pages, exit 0), ESLint (only the two pre-existing `<img>` warnings) and `git diff --check`. The real workflow runs against in-memory DB/Sheets fixtures through six scenarios, including complete write/readback, retry after a crash before control, an occupied metric range and an unknown future size. Tests preserve a manual 17-unit receipt, an older order row and the original total-order formula while registering five names, nine WB snapshots and the separate cumulative metrics. No live mutation was used for verification.

### Production rollout and verified recovery

- The owner-authorized Deploy #15, GitHub Actions run `34123140699`, succeeded for exact commit `c04011a49e72cdae7ad141f551f6da0f7b41100c`. Runtime verification found the app and both workers using that image; the application health endpoint returned HTTP 200.
- The pre-release backup `nimba-production-20260907-124812-c04011a49e72.dump` is 10,742,032 bytes, SHA-256 `ECA8F12301E9608FBFF498B42AD09D057FD87F2017837330CAED0BDC6B1B23AB`, with a readable archive table of contents. The migration journal contains 16 completed migrations and one old rolled-back attempt, with zero unfinished migrations. The release reapplied 45 sync and 3 automation schedulers.
- Authorized current-stock jobs succeeded: Nimba `397da6dc-3dc6-4667-92aa-e019cec63011` and Galioni `ffdc5885-4e66-48b6-adde-a98ae8cc64b5`. They reported 294 and 306 WB units respectively (600 total) and updated 38 and 33 assortment rows (71 total), with zero price rows, discovered items or errors. These were current-stock refreshes, not historical resynchronization; Sheet projection uses its own enabled/zero-stock rules.
- The first FBS Sheet run `7511f0f9-4778-4e3b-83c8-ce7d5702b218` succeeded at `2026-09-07T12:55:30Z` for target `2026-09-06`: five reference names registered, nine WB stock rows inserted and 55 updated, four operation rows inserted and 18 updated. Nomenclature registration itself creates no opening balance or physical movement.
- Actual metric readback is `Сводка!K5 = не менее 635`, `L5 = не менее 517`, with period `28.07.2026–06.09.2026`. The result remains `INCOMPLETE` because 23 legacy Sheet-date diagnostics were found and investigated read-only; the display therefore preserves lower bounds rather than claiming exact totals. All 23 are historical date-bucketing mismatches in rows 120–439: Sheet dates July 30–August 8 versus DB Moscow dates July 31–August 9, entirely inside the retained cohort. Twelve match the UTC date and eleven do not; the original conversion is not established, so this is not a universal UTC/timezone explanation. No cohort boundary is crossed and missing-order/supply diagnostics are zero. Historical dates were not rewritten and such a rewrite is not authorized. There are 16 physical-stock reconciliation warnings, including the five newly registered groups with zero physical stock. These warnings are not permission to manufacture receipts or alter quantities.
- The live mapping review reports zero unmapped items. The configured 12:05 and 23:30 schedule times are unchanged. Historical Excel receipts remain candidates requiring review, and the 118 orders without saved individual acceptance evidence remain unresolved; see the dated analysis in `docs/marketplace/MARKETPLACE_ANALYSIS_LOG.md`.
- Independent comparison against the pre-write snapshot passed across all eight tabs with `protectedChanges=[]`: 99 manual rows, 37 opening/receipt rows, 3,007 Summary formulas and 1,054 daily-report formulas were preserved, as were archives, local `onHand`/`reserved` balances and workflow configuration. The five reference additions occupy `A51:A55` and project into Summary rows 55–59; their nine tuples retain 86 WB units and zero physical stock. All 55 existing barcode cells changed only from numbers to RAW strings containing the same digits.
- Presentation/readback checks confirmed six notes in `K4:L6`, row 4 at 60px, no new merges and preserved borders. The original `G5` total-orders formula remains unchanged and now evaluates to 650; `Справочники!E12` is September 6. The current-stock total is 600 WB units across 64 projected tuples.
- The second FBS run `9f2d3283-5348-4a94-91c5-fce8268a1b45` succeeded at `2026-09-07T13:03:32Z`: zero operation inserts/updates with 293 unchanged, zero stock inserts/updates with 64 unchanged, no new names, 600 WB units and the same 16 reconciliation warnings. Final strict comparison also passed: between the two post-run snapshots only 56 control timestamps in column G changed; operations, stocks, reference names, Summary, metric notes and formatting are identical, with zero duplicates. The before-to-final comparison found zero protected-cell changes and preserved all checked balances, configuration, manual rows, formulas and archives. BUG-037 is closed after the independent first-run readback, actual idempotent rerun and final strict snapshot verification.
- The separate marketplace analysis diff is preserved in its canonical journal. Its Excel receipts remain candidates and its 118 acceptance-evidence gaps remain unresolved. This documentation follow-up does not require another runtime deploy.

## 2026-09-04 - Reduced Graphify memory overhead

- Replaced mandatory graph-first behavior with a read-only-by-default workflow: scoped tasks use direct sources, while broad architecture/dependency questions may use one bounded query before source verification.
- Removed the pre-tool Graphify hook and shortened the project skill from a full maintenance manual to a lightweight mode router. Ordinary work no longer loads semantic/update instructions or waits for graph synchronization.
- Moved semantic extraction, clustering, labeling and HTML generation into explicit batch maintenance. Code-only maintenance uses deterministic AST update without clustering by default.
- Disabled normal `save-result`/`reflect` usage because upstream Graphify intentionally re-ingests `graphify-out/memory`, which previously created a self-referential second update.
- Relaxed session startup so handoff/current-task files are loaded only for continuity or status work, not every isolated task.
- Added `docs/core/GRAPHIFY_MEMORY_GUIDE.md` with simple user prompts, maintenance cadence and warning signs. No application, database, production or WB state was changed.

## 2026-09-04 - Fixed daily sync and morning-report scheduler skips

- Investigated production on mini-PC `WIN-SK69NVLD6F0` after all non-FBS nightly syncs were skipped on September 3 and product-card refresh was skipped again at 02:00 on September 4. Host, Docker, PostgreSQL, Redis and both workers were continuously healthy; the failure was not sleep, reboot or worker downtime.
- BullMQ evidence showed affected jobs completed instantly with `scheduled job belongs to a replaced schedule`; the September 3 morning-report job likewise returned `scheduled workflow configuration has changed`. Because these skips happen before run creation, they were absent from `SyncJobRun`/`AutomationRun` history and looked like missed launches.
- Root cause: legacy sync settings and the morning workflow had no stored extended `schedule`. Normalization filled the unused `anchorDate` with the current Moscow date, while `flexibleScheduleFingerprint()` serialized every field. The fingerprint therefore changed at midnight. Fully saved FBS schedules had a fixed anchor date and continued working.
- Changed schedule fingerprints to include only effective fields for the selected cadence and time mode. Daily fingerprints ignore unused anchor/week/month fields; weekly, monthly and every-N-weeks fingerprints still include the fields that affect their execution. Added regression coverage for stable daily fingerprints and meaningful every-N-weeks changes.
- Before 02:30, re-applied production schedules from the running release container so the remaining September 4 jobs were protected. Corrected the manual recovery commands to execute scheduler scripts in the verified running release rather than an unpinned compose `latest` image.
- Local type-check, 71 tests, lint and `git diff --check` passed; lint retains the two known `<img>` warnings. CI run `33817010275` and production workflow `33817076564` completed successfully. Production now runs image/commit `fdb3f5e0b3e8808b455a2f967c3b2680834a7a80`; app/PostgreSQL/Redis are healthy and both workers are running.
- Live verification at 02:30 confirmed `reports.period` created a real `RUNNING` `SyncJobRun` instead of a silent skip. Three current, read-only `products.refresh` jobs were queued to recover the missed 02:00 refresh. No historical resync, WB write, schema migration, environment change or volume operation was performed.

## 2026-09-02 - Refreshed local development database from production

- With explicit owner request, repeated the controlled mini-PC snapshot workflow. Verified remote host `WIN-SK69NVLD6F0`, user `n8929`, checkout `C:\NimbaOS\nimba`, compose project `nimba`, database `nimba_production`, container identities and local application health before export.
- Read-only verification found production, local `main` and `origin/main` at commit `e5c5a5af7b3ff88c858767c177de49434b81c6f3`. Production and the repository both contain 16 completed migrations plus the expected historical rolled-back attempt; the previous handoff statement that the September 1–2 rollup still awaited deployment was stale and was corrected.
- Stopped only the running local Next.js development process; local sync and automation workers were not running. Created a custom-format `pg_dump`, validated archive readability, size and SHA-256 on the production container, mini-PC and laptop, and restored it first into isolated database `wb_cabinet_refresh_20260902_194618`.
- Validation found zero unfinished migrations, invalid indexes or unvalidated constraints. Final local controls are 2 users, 3 WB accounts, 171 products, 14,298 orders and 78,601 realization-report rows; order/report coverage reaches 2026-09-01 and the latest successful sync timestamp in the snapshot is `2026-09-02 15:00:47.761`.
- Final FBS controls are 625 orders, 1,021 order events, 8 inventory movements, 614 KIZ units, 4,249 KIZ events, 551 compliance tasks, 7 operation batches and 0 FBS action logs. Latest FBS/KIZ timestamps in the snapshot are from 2026-09-02.
- Switched the validated database into the standard local `wb_cabinet` name, confirmed `prisma migrate status` reports the schema up to date, then removed the superseded local database and all temporary dump copies from both hosts and containers. Production remained online and healthy (HTTP 200); no production data, Redis state, worker, scheduler, code or WB state was changed.

## 2026-09-02 - Permanent Graphify memory workflow

- Replaced the minimal Graphify notes in `AGENTS.md` with a permanent project-level workflow: graph-first navigation, scoped source reading, source-of-truth precedence, decision capture, incremental memory updates and post-update verification.
- Made Graphify usage automatic for relevant work; users no longer need to invoke `/graphify` explicitly.
- Defined completion semantics: graph-relevant work is not complete until the canonical source and Graphify are synchronized, or the graph is explicitly reported stale with a reason.
- Kept routine non-knowledge edits outside the rebuild requirement to avoid unnecessary complexity and graph churn.
- No application source, business logic, database, WB data, Google Sheet or production state was changed.

## 2026-09-02 - Initial deep Graphify knowledge graph

- Built the initial project knowledge graph from 320 relevant sources: 283 code/configuration files and 37 active documents; dependencies, builds, caches, generated artifacts, secrets, raster assets and `docs/archive/` were excluded through `.graphifyignore`.
- Enabled the official `graphifyy[sql]` extra so all 17 SQL migration/analysis files participate in structural extraction.
- Combined AST extraction with deep semantic extraction of architecture, data policy, marketplace methodology, KPI definitions, decision rules, current state, incidents and operating runbooks.
- Added evidence-backed semantic bridges between documentation and concrete services for WB sync/reporting, FBS/KIZ/Google Sheets, authentication/authorization and marketplace KPIs.
- Generated `graphify-out/graph.json`, `graphify-out/graph.html` and `graphify-out/GRAPH_REPORT.md`; verified JSON/HTML/report integrity plus bounded queries, node explanation and cross-domain shortest paths.
- Final graph after DB-first navigation audit: 2,879 nodes, 7,736 edges and 177 labeled communities. The audit added verified table-to-service and KPI-formula links and made the standard report's live-ad-cost option explicit. Benchmark estimates 15.3x fewer tokens per typical graph query than naive corpus reading.
- No application source, business logic, database, WB data, Google Sheet or production state was changed.

## 2026-09-02 - Project-scoped Graphify installation

- Confirmed Python 3.13.7 and `uv` 0.11.3 on the local Windows host.
- Installed official `graphifyy` 0.9.53 as an isolated `uv tool`; the `graphify` and `graphify-mcp` executables are available from the user tool bin.
- Registered the Graphify skill only for Codex project scope at `.codex/skills/graphify/`, added `.codex/hooks.json` and the generated Graphify guidance in `AGENTS.md`.
- Verified `graphify --version`, the installed skill version, hook execution and project files. The knowledge graph itself was intentionally not built in this task.
- No application source, business logic, database, WB data, Google Sheet or production state was changed.

## 2026-09-02 - Compact current/archive documentation memory

- Replaced oversized development/marketplace handoff and current-task files plus `PROJECT_STATE.md` with compact current snapshots. Original files moved unchanged to `docs/archive/snapshots/2026-09-02-pre-cleanup/`.
- Split active decisions and active bugs from complete historical ledgers; original ledgers remain in the snapshot.
- Moved obsolete Claude prompts and the completed dashboard roadmap into explicit archive folders.
- Rebuilt `docs/DOCS_INDEX.md`, corrected stale open questions, updated the project map, and changed documentation rules from mandatory multi-file duplication to one canonical detailed log plus bounded current state.
- Archived the pre-cleanup development and marketplace logs as closed historical segments; new entries continue at their canonical paths.
- No application code, database, environment, WB data, Google Sheet or production state was changed.
