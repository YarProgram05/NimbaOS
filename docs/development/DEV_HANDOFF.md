# Dev Handoff

## Current Development Objective

Поддерживать реализованный MVP NimbaOS, довести Phase 7/8 live WB verification и future production rollout без нарушения safety rules.

## Last Development Session Summary

2026-09-01: completed the full phone-responsive pass across NimbaOS. The shared shell now uses `100dvh`, moves the desktop sidebar boundary to `lg`, closes the mobile drawer after navigation/account changes and keeps every navigation item reachable inside a safe-area-aware scroll container. Shared controls provide phone-sized touch targets, viewport-safe dialogs/sheets/popovers, vertical page scrolling over tables and a one-month mobile date picker with a persistent action footer. Table-only workflows now expose compact mobile cards, sorting and nearby actions for products/prices, financial reports, stocks and sizes, reviews/questions, references, sales plans, FBS orders/assortment/supplies, sync and automation history, WB accounts and admin users; dense desktop tables remain unchanged at `lg+`. Auth pages, advertising detail, analytics, settings and schedule editors were also hardened for long text, keyboard-safe inputs and narrow/short viewports. Authenticated QA used only the Codex in-app browser at 340 px portrait, 638/744/897 px landscape/tablet boundaries and 1531 px desktop, including navigation, nested dialogs, date/calendar controls, product price editor, schedule drawer and dynamic advertising/analytics pages. No document-level horizontal overflow or console errors remained. `npm run type-check`, `npm run lint`, all 60 tests and `git diff --check` pass; lint retains only the two existing `<img>` warnings. A production build was intentionally not run while the verified local `next dev` was active, per `BUG-031`. No save, delete, sync, worker, WB write, database or production action was performed.

2026-09-01: diagnosed the production `Проверить подключение` Google Sheet failure after deployment of `c57f120`. Read-only mini-PC checks verified the expected host and commit, healthy app/workers and HTTP 200 without reading any secret. `GOOGLE_SERVICE_ACCOUNT_JSON_BASE64` was present in `automation-worker` but missing from `app`: Compose injected it only into the worker even though the inspection server action executes in the Next.js app. Added the existing environment variable mapping to the production `app` service; no `.env.production` change or credential rotation is required. The fix requires the normal owner-confirmed production redeploy before browser verification.

2026-09-01: replaced the initial static Full HD dashboard fit with a genuinely height-responsive layout. On `xl` desktop widths, the home page occupies the available main height; its middle chart/focus row is flexible, so it grows in browser fullscreen and shrinks in a normal Chrome window. At viewport heights below 1000 px, only spacing and panel padding become denser; no dashboard metric or detail row is hidden. Below `xl`, forced height distribution is disabled and chart, distribution, focus and overview panels return to natural stacked height, preventing the overlap found at a 990x909 viewport. The compact chart fills the flexible row only on desktop, while the separate full analytics chart remains 432 px. Authenticated QA measured exact 1920x952 normal-window, 1920x1001 breakpoint and 1920x1080 fullscreen viewports with no vertical/horizontal overflow; 990x909 had zero overlap between every adjacent page, middle-panel and chart-part pair, no horizontal overflow and normal vertical scrolling. Console remained clean. Type-check, lint and all 60 tests passed; lint retains only the two existing `<img>` warnings.

2026-09-01: implemented the owner-selected compact catalog plus right-side editor for `/sync` schedules. The former forced-width editable table was removed; eleven jobs are grouped into core data, reports, advertising, feedback and operational FBS cards with enabled state, readable schedule, contextual data depth and next run. One drawer edits a task at a time through the same extracted flexible schedule component used by automations. Every sync kind now has the same time controls: daily or selected weekdays, one/several exact times, or a bounded start/end/every-minute interval. No per-kind interval permission or interval limit exists; only snapshot versus rolling-depth presentation remains contextual. The drawer previews the next three Moscow runs, warns about coincident times and restores per-kind recommendations. Added nullable `SyncScheduleSetting.schedule` JSON with migration `20260901143000_flexible_sync_schedules`; legacy `timeOfDay`/`intervalMinutes` rows remain readable until normal save. BullMQ now registers an exact scheduler per effective time and scheduled payloads carry a fingerprint/time so replaced or legacy queued jobs are skipped. The migration was applied only to confirmed local `localhost:5432/wb_cabinet`; no schedule was saved/reapplied and no sync/worker/WB/production action occurred. Browser QA found zero horizontal overflow and no console errors; interval mode was verified for both ordinary and FBS syncs, then canceled. Verification passed 60 tests, type-check, lint and a full production build with only the two existing `<img>` warnings.

2026-09-01: completed a read-only UX and scheduling-model audit for the next `/sync` schedule-settings redesign. The current UI exposes eleven live-edit controls in a forced `min-w-[820px]` table; at the normal authenticated 1074 px workspace, the right action column is outside the visible card and requires horizontal scrolling. It also mixes daily snapshot jobs, rolling-period jobs and always-on FBS intervals under the same ambiguous `Период` column. The backend currently supports only one daily `timeOfDay` or one unrestricted `intervalMinutes`; it does not yet support the automation editor's multiple times, weekdays or bounded interval window. Recommended UX is a grouped compact catalog with one selected task edited in a side drawer, using a shared schedule engine constrained by per-sync-kind capabilities. No settings were saved, no schedule/job/worker was started and no source code was changed; implementation awaits owner selection among the proposed UX variants.

2026-09-01: implemented the shared schema-driven Google Sheet and cabinet settings template for both automation detail pages. FBS now persists one `sheetTabs: role -> actual tab` mapping instead of five individual tab-name fields; stable role definitions, labels and validation live in reusable metadata. A centralized adapter reads existing legacy JSON fields until the owner next saves, after which the new format is persisted. The shared UI scans the connected workbook read-only, auto-matches defaults, offers detected names while retaining manual entry, validates missing/duplicate/renamed tabs and cabinet mappings, hides FBS technical keys under advanced settings, and blocks save on known configuration errors. The scan now explicitly lists every detected tab with `Используется` / `Не используется` status and found/used/free counts; unused tabs are neutral and immediately selectable for an existing role. Authenticated local QA read the real FBS workbook (8 found, 5 used, 3 free) and morning workbook (3 found, all used), verified the missing-tab guard, advanced controls and zero horizontal overflow at a 1074 px viewport. No settings were saved and no automation was run. All 57 tests, type-check, lint and production build passed; only two pre-existing `<img>` warnings remain.

2026-09-01: standardized the complete run-history tables on `/automations` and `/sync`. Both now use the same responsive full-width fixed layout with percentage-based columns, matching 64 px collapsed cell height, two-line truncation for every informational column and a shared click-to-expand cell component that reveals the complete value and toggles closed again. `/sync` now uses the same `max-w-7xl` content width as `/automations`; its delete action remains separate from expandable data cells. After the first 1440 px draft caused horizontal scrolling, it was replaced with the adaptive layout. Authenticated browser QA at a 1394 px viewport confirmed both 1036 px tables exactly match their containers with no horizontal overflow, all headers remain readable, normal rows are 65 px including borders, and long automation/sync errors expand to their complete text. Type-check, lint, all 52 tests and production build passed; only the two pre-existing `<img>` warnings remain. No run, sync, schedule, worker, WB API or production mutation occurred.

2026-09-01: corrected a second owner-confirmed duplicate WB listing in the FBS Sheet workflow. Stable tuple `nimba:232092449:366203604` (`парео синяя полоска`) now maps to the existing canonical physical product `туника синие волны`, matching the already-accounted Galioni card `219179076:348718974`. Updated the code default and local workflow config, cleared the accidental separate reference entry, and relabeled the existing Nimba WB-stock row without changing its technical key or quantity. Live Summary now shows the canonical row with WB stock 10 Nimba + 15 Galioni; the retired name is absent from active tabs. Dry-run remained a no-op with 255 unchanged events, 53 unchanged stock rows, 542 WB units and zero failed accounts. All 52 tests, type-check and lint passed; browser QA confirmed the merged row remains readable.

2026-09-01: diagnosed failed GitHub Actions CI run `33449872826` for commit `b350c8b`. All checks passed until `next build`; static generation attempted `/automations` Prisma calls without a CI PostgreSQL service and failed with `ECONNREFUSED`. Marked both automation routes `force-dynamic`; the full local CI sequence passed and the route table confirmed dynamic rendering. Fix commit `aa645e7` was pushed to `main`, and GitHub Actions CI run `33450459678` completed successfully in 2m 49s.

2026-09-01: completed the local implementation of `Заказы FBS → таблица учета`. Added Prisma kind/migration, catalog/detail settings, flexible schedule integration, queue execution, stable-key upsert, pre-handoff cancellation offsets, explicit `RETURN_RECEIVED` handling, Moscow-date reconciliation, control-row readback and formula/schema/sync-coverage guardrails. Product mapping uses account/nmId/chrtId and includes the owner-confirmed alias `nimba:297175085:452136209 → туника леопард/пятна`; the erroneous separate reference value was removed. The authorized initial live-sheet load through 2026-08-31 inserted 250 events and updated 5 existing rows; the immediate retry preview returned 0 inserts, 0 updates and 255 unchanged events. Connector readback exposed UTC wall-time serialization in the load timestamp; the code and all 255 initial timestamps were corrected to Moscow time and a no-op workflow rerun passed. The local workflow setting is enabled at 23:30 Europe/Moscow and was applied locally; no production deploy, production migration, production worker or scheduler change was made. `npm test` passed 49 tests, type-check and lint passed (two pre-existing img warnings), production build passed, and authenticated browser QA on a freshly restarted port 3000 confirmed intact styles and the automation catalog.

2026-09-01: redesigned `/automations` into a workflow catalog plus shared run history. The existing `Утренний отчет WB` settings moved to `/automations/morning-wb-report`; the catalog card shows status, readable schedule and next run. Added a generic JSON-backed schedule model supporting one or many exact times, intervals within a day, selected weekdays, every N weeks with an anchor week, and selected month days. BullMQ now registers one scheduler per effective time, while every-N-weeks jobs are gated by the worker against the saved week cycle. Existing daily `timeOfDay` rows remain backward compatible and no Prisma migration was needed. Automation history now displays the registered workflow name and supports server-side name sorting/filtering. Type-check, lint, 40 tests and production build passed; authenticated browser QA covered the catalog, detail route and advanced schedule controls without saving data. The local dev server was restarted after the build; no worker, schedule, automation run, WB API or production mutation occurred.

2026-08-31: resolved local unstyled NimbaOS pages after browser QA. The application stylesheet returned 404 because `npm run build` had replaced `.next` while the existing `next dev` still referenced its old generated CSS URL. Verified the port-3000 process belonged to this checkout, restarted only the local dev server, and reloaded the authenticated in-app browser. CSS now returns HTTP 200 (106,095 bytes, 164 rules) and normal layout is restored. No database, worker, queue or production process was touched. `BUG-031`, commands and troubleshooting now document that dev and build must not run concurrently in one checkout.

2026-08-31: replaced the fixed 50-row histories on `/sync` and `/automations` with complete database-backed archive navigation. `SyncJobRun` and `AutomationRun` remain the canonical stores; the UI now requests filtered/sorted 25/50/100-row pages with exact totals, previous/next navigation and debounced column filters instead of loading a truncated client-side list. After UI review, redundant text-period and separate date inputs were removed: both pages reuse the canonical two-month `DateRangePicker` with quick presets as the single task creation/run date filter. Sync history additionally filters type, status, account, source and error; automation history filters status, source and error. Read-only local smoke checks found 789 sync runs and 49 automation runs. Both pages and calendar application were visually verified in the authenticated in-app browser. Type-check, lint, 36 tests and production build passed; only the two existing unrelated `<img>` warnings remain.

2026-08-31: refreshed the laptop's local `wb_cabinet` development database twice from newly validated read-only snapshots of the mini-PC production PostgreSQL database. The second refresh captured same-day FBS changes that landed after the first snapshot. Each snapshot was restored and checked in an isolated local database before switching names; all 13 repository migrations matched, the one historical rolled-back migration remained expected, and no active unfinished migration, invalid index or unvalidated constraint was found. Final local core counts are `users=2`, `wb_accounts=3`, `products=171`, `wb_orders=14247` and `realization_reports=78225`. Final FBS controls are `fbs_orders=613`, `fbs_order_events=958`, `fbs_inventory_movements=8`, `kiz_units=604`, `kiz_events=4197`, `kiz_compliance_tasks=541`, `kiz_operation_batches=7` and `fbs_action_logs=0`; the latest FBS/KIZ updates in the snapshot are from 2026-08-31. Production stayed online and healthy throughout; its code, data, containers, Redis, workers and schedules were not changed. Obsolete local databases and all temporary snapshot copies were removed after verification. Local PostgreSQL and Redis are healthy; local sync and automation workers remain stopped.

2026-08-27: added the canonical mini-PC production operations guide at `docs/core/MINI_PC_RUNBOOK.md`. New agents now have one stable map for local-vs-production identity, Tailscale/OpenSSH preflight, Docker services and persistent state, read-only diagnostics, controlled releases, backup/restore boundaries, Windows session caveats and approval gates. `AGENTS.md` and `docs/DOCS_INDEX.md` route production infrastructure work to it; mutable release/incident details remain in the existing state and handoff documents.

2026-08-27: refreshed the laptop's local `wb_cabinet` development database from a validated read-only snapshot of the mini-PC production PostgreSQL database. The snapshot was restored and checked in a separate local database before switching names; all 13 committed migrations matched, the one historical rolled-back migration remained expected, and no active unfinished migration, invalid index or unvalidated constraint was found. Final local control counts are `users=2`, `wb_accounts=3`, `products=171`, `wb_orders=14161` and `realization_reports=77276`. Production stayed online and healthy throughout; its code, data, containers, Redis, workers and schedules were not changed. Temporary snapshot files were removed from both hosts and containers. The local Next.js dev server is running at `http://127.0.0.1:3000`; health, login HTML/CSS and unauthenticated redirects for the main read-only routes passed. Local sync and automation workers remain stopped.

2026-08-24 correction: the earlier Worker workaround did not make public access reliable and has been removed from `app.nimbaos.ru`. The domain again points to the native Cloudflare Tunnel; `/api/auth/csrf` and credentials handling are correct, and an invalid credentials request returns HTTP 401 quickly. The remaining login symptom is missing Next.js hydration: from the affected Russian IPv4 client, a cached 124,727-byte JavaScript chunk repeatedly stops at about 24,576 bytes, while the same file completes locally, through Tailscale and through the mini-PC's IPv6 path. Explicit small byte ranges also complete, matching the documented Russian ISP throttling of Cloudflare response bodies. Cloudflared is restored as an automatic Windows service using QUIC over IPv6, but no Cloudflare connector or Worker setting can bypass throttling between Cloudflare and the user. Current reliable access is `https://win-sk69nvld6f0.tailc11887.ts.net` on trusted Tailscale devices or `http://127.0.0.1:13000` through the laptop SSH tunnel. The preferred no-VPS public design is a public/static IPv4 from the ISP, direct HTTPS on the mini-PC and DNS-only Cloudflare records.

2026-08-23: restored stable public access to the mini-PC application. A normal ZTE LAN1 -> Keenetic port 0 connection now gives the Keenetic Ethernet uplink `192.168.1.19` and is the primary internet path; WISP remains available as reserve. The native Cloudflare Tunnel still drops its edge connections on this provider path, so production ingress no longer depends on that connector. Enabled persistent Tailscale Funnel for `https://win-sk69nvld6f0.tailc11887.ts.net` -> `http://127.0.0.1:3000`, published Cloudflare Worker `nimbaos-proxy`, and attached the Worker custom domain `app.nimbaos.ru`. The resulting path is `app.nimbaos.ru -> Cloudflare Worker -> Tailscale Funnel -> local Next.js app`. Public `/api/health` passed 20/20 checks over more than two minutes, `/` redirected to login, and `/login` returned HTTP 200 with the expected form. A follow-up browser check found that the custom hostname stalled while sending larger `/_next/static/` response bodies, leaving the login page unstyled. The Worker now redirects only immutable Next.js static assets from `app.nimbaos.ru` to its technical `nimbaos-proxy.yaros-05.workers.dev` hostname; application pages, API calls and authentication remain on `app.nimbaos.ru`. The production CSS completed at the exact expected size of 78,262 bytes in 1.32 seconds, and a clean Chrome session rendered the styled login page correctly. Docker app/PostgreSQL/Redis and both workers are running; AnyDesk and Tailscale remain automatic. The old Cloudflared service is retained temporarily for diagnosis/rollback but its Tunnel DNS record was removed and it is not in the production request path. Worker source is tracked under `infra/cloudflare/nimbaos-proxy/`.

2026-08-23: completed the controlled ZTE LAN -> Keenetic Internet/WAN test. When the new Ethernet uplink was promoted to primary, Keenetic received `10.49.27.58/21` with gateway `10.49.24.1` instead of the expected ZTE LAN subnet `192.168.1.x`. That path also returned a filtered/private Cloudflare DNS answer (`172.16.24.100`), and Netcraze RMM, Tailscale, Cloudflared and AnyDesk became unreachable. Disconnecting the new cable restored the WISP route, Tailscale/RMM access and local application health. AnyDesk initially kept retrying the poisoned `172.16.24.100` answer; clearing the Windows DNS cache and fully restarting its service restored a stable TLS relay connection on public IPv4/443 and incoming managed-client access. Cloudflared was restarted and registered four HTTP2 edge connections, but sustained public checks still alternated among HTTP 200, 530 and timeouts, so `BUG-026` remains unresolved. The Keenetic Ethernet uplink profile was returned to `Reserve 1`; WISP remains primary. Reconnect the next candidate ZTE LAN port only while Ethernet remains reserve, verify that it supplies `192.168.1.x`, and promote it only after sustained DNS, Tailscale, RMM, AnyDesk and public-health checks pass.

2026-08-23: continued the Keenetic/Cloudflare diagnosis in the router UI and on the mini-PC. Registered the mini-PC as a permanent Keenetic client at `192.168.2.82`, changed the active WISP uplink profile `local` from on-demand to always-on, and updated the official Windows `cloudflared` binary from `2026.5.2` to `2026.8.2` while preserving the previous executable as a rollback copy. The owner then updated Keenetic from developer firmware `5.2 Alpha 5` to `5.2 Alpha 6`; RMM confirmed the router online after reboot. A four-minute post-update test returned five HTTP 502 responses followed by nineteen HTTP 530 responses, while local `/api/health` remained HTTP 200 and Cloudflared again logged disconnected control streams. The firmware update therefore did not resolve `BUG-026`. The mini-PC is wired to a Keenetic LAN port, but Keenetic itself still reaches ZTE over WISP Wi-Fi; the wired server link does not remove that upstream hop. Preferred next test: connect a second cable from a ZTE LAN port to Keenetic Internet/WAN, make Ethernet the primary Keenetic uplink, and disable or demote WISP.

2026-08-23: retested mini-PC production ingress after connecting the server to Keenetic by 1 Gbps Ethernet and disabling Wi-Fi. Docker PostgreSQL, Redis, app, sync worker and automation worker were all restored and healthy; local `/api/health` stayed HTTP 200. The active route is still `192.168.2.1 -> 192.168.1.1 -> 100.90.0.1`, so Ethernet removed Wi-Fi from the failure path but did not remove ZTE double NAT or MGTS CGNAT. Cloudflared HTTP2 produced one HTTP 200 before its control streams closed and public requests returned 502/connection resets; QUIC registered four replicas but hit `timeout: no recent network activity` within about five seconds. The Windows Cloudflared service was returned to HTTP2/IPv4, set to delayed automatic startup, and given restart recovery actions after 5/15/30 seconds. This prevents a one-time DNS/network-startup race from leaving the service stopped, but it does not make the current provider path production-ready. Request public/static IPv4 or use a VPS reverse proxy before relying on `app.nimbaos.ru`.

2026-08-21: resolved and live-verified `BUG-029`. Same-time account jobs still run serially with sync concurrency 1, but the shared sync/automation start-grace policy is now 1080 minutes by default and capped at 1439 minutes. Commit `bb139f9` is deployed; both production workers expose the 1080-minute value and are running normally. The approved bounded Galioni advertising sync for 2026-08-07 - 2026-08-21 succeeded with zero campaign errors and advanced coverage through 2026-08-21. The subsequent morning-report run for target 2026-08-20 succeeded for both accounts, wrote 20 rows to each mapped sheet, and reported zero failures. Redis retains 14 enabled sync schedulers (seven kinds for two accounts; sales-plan settings are explicitly disabled) plus one morning-report scheduler for the next cycle.

2026-08-21: deployed the separate production automation runtime on the mini-PC. `nimba-automation-worker-1` now runs continuously inside the `nimba` compose project, receives the Google service-account credential from untracked `.env.production`, and consumes the BullMQ `automation` queue. Re-applied the one enabled schedule; Redis retained one future delayed job and had zero waiting/active jobs when the worker started. The temporary one-shot scheduler container was removed. Initial deployment deliberately avoided a report while coverage was incomplete; after `BUG-029` recovery, an owner-authorized live run succeeded for both mapped sheets. Manual automation and sync actions enqueue immediately and are consumed by their respective workers; they do not wait for schedule time.

2026-08-21: restored production BullMQ sync schedules on the Windows mini-PC. The PostgreSQL restore preserved two enabled `PRODUCTS_REFRESH` rows at `02:00 Europe/Moscow`, but Redis contained no `sync` job schedulers, so the worker had nothing to execute. Re-applied all database-backed schedules through the existing `nimba-app` image: 22 settings were processed for two active accounts and 16 enabled schedulers/delayed jobs were registered. Manually queued the missed product-card sync for both cabinets; `WB Nimba (WB_1)` succeeded with 87 updated cards and 131 price rows, and `WB Galioni (WB_2)` succeeded with 63 updated cards and 63 price rows, both with zero errors. After a Redis data-volume restore/recreation, re-run the schedule application command before relying on nightly automation.

2026-08-14: brought up the Windows 11 mini-PC production stack and diagnosed blocked public ingress. PostgreSQL, Redis, the Next.js app and the sync worker are running under Docker; `http://127.0.0.1:3000/api/health` returns 200 and the restored database is intact. The mini-PC now uses Keenetic at `192.168.2.147`, but `tracert` proves the path is still double-NATed (`192.168.2.1 -> 192.168.1.1 -> 100.90.0.1`), so ZTE is not operating as a transparent bridge and the provider remains behind CGNAT. Cloudflare Tunnel registers four TCP/HTTP2 connections on port 7844, then public checks degrade from 200/502 to 530/1033 while local sockets can remain falsely `Established`. The same result was reproduced with the current Windows connector, a newer Docker connector, QUIC, HTTP2, the normal DME edge and forced US `ewr/ord` edges; router firewall/Anti-DoS changes did not help. The original global Cloudflared Windows service configuration was restored and is running automatically. AnyDesk was installed from portable mode into `C:\Program Files (x86)\AnyDesk` as the automatic `AnyDesk` LocalSystem service; AC sleep and hibernation are disabled. The operator must still set and test an unattended-access password locally. LAN SSH remains available at `n8929@192.168.2.147`, but direct SSH from another network still requires Tailscale/VPN or another secure tunnel. Next safe decision: request a public/static IPv4 from MGTS and retest, or use a small VPS reverse proxy; do not treat current Cloudflare Tunnel as production-ready.

2026-08-13: fixed late historical FBS withdrawal tasks for post-handoff cancellation/defect in both cabinets. Official CRPT guidance still requires remote-sale withdrawal at shipment (within 3 working days and before delivery), but when NimbaOS first learns a historical order only after it has already become `canceled_by_client`/`defect`, and the withdrawal was never confirmed, it no longer creates or exports the operationally pointless `withdraw -> return` pair. Pending OPEN/EXPORTED withdrawal tasks are canceled with an audit event, the local circulation state stays `IN_CIRCULATION`, and physical state stays `RETURN_EXPECTED` until receipt inspection. Confirmed withdrawals are never changed and still require return-to-circulation after physical return. Local reconciliation canceled 28 Nimba and 40 Galioni unconfirmed tasks; final audit showed zero open withdrawal tasks attached to canceled orders in both cabinets. The new export query also excludes any canceled order as a fail-safe. Tests 36/36, type-check and production build passed; no WB/CRPT call or mutation occurred.

2026-08-13: replaced the temporary 100-row FBS history window with server-side archive navigation. Orders, KIZ units, CRPT tasks, supplies and the WB action journal now search/filter/sort across the complete account-scoped database history while returning only the requested 25/50/100-row page. A shared optional history date range applies to all five journals; the browser never receives the complete archive. Encrypted KIZ search remains application-side after bounded account/date/status selection so full codes stay encrypted at rest. Analytics search is compact and shares its row with total direct OP, margin, profitability and buyout percentage; article rows expose the same profitability/buyout metrics. Profitability is `OP / total expenses`; buyout is `net sales / (sales + cancellations)` over completed outcomes. Real local DB smoke checks found results beyond the first 100 rows; tests 35/35, type-check and production build passed with only two old unrelated `<img>` warnings.

2026-08-13: completed the FBS history/table/analytics follow-up. `/fbs` keeps all database history but bounds each heavy workspace journal to an operationally prioritised window of up to 100 rows, renders 25 rows per page (10 for the WB action journal), shows exact database totals, and supports ascending/descending sorting from every table header. The standalone UNKNOWN-circulation explanation was removed. Analytics now uses the shared date-range calendar and article rows include orders, cancellations, buyouts, returns, revenue, transfer, direct FBS operating profit and margin. Direct FBS OP is `transfer - direct WB realization expenses - COGS - tax`; shared advertising is deliberately not allocated between FBS/FBO and the UI says so. Exact KIZ unit `5a118244-fb2b-40bb-994a-02f00f040144` was verified against the user-supplied identification code, changed once from `IN_CIRCULATION` to `WITHDRAWN` in a transaction, and an audit event was recorded against its sold order. No WB/CRPT write, sync, deletion, migration or production operation was run. Tests 35/35, type-check and production build passed; only two pre-existing unrelated `<img>` lint warnings remain.

2026-08-13: corrected the CRPT withdrawal XLSX contract. Future withdrawal exports now contain `Код маркировки` plus numeric `Цена за единицу с НДС`, taken from the linked FBS order `convertedPriceRaw / 100`; no VAT is added because the seller uses VAT 0%. Return-to-circulation files remain code-only. Missing/zero order price blocks export instead of silently producing an invalid CRPT file. Four previously downloaded withdrawal workbooks were rebuilt as separate `_с_ценами.xlsx` copies (9, 16, 163 and 179 rows), all rows matched, all codes were canonicalized to `01GTIN21serial`, and originals were preserved. A bounded read-only audit also proved that 57 `IN_STOCK + UNKNOWN` units (29 Galioni, 28 Nimba) were historical pickup-cancellation/defect KIZs imported from WB metadata, not unassigned warehouse stock; no DB/sync/WB/CRPT mutation was performed. Tests 35/35, type-check and lint passed; lint retained two unrelated `<img>` warnings.

2026-08-12: fixed `BUG-024` in the FBS KIZ lifecycle and completed cross-tab filtering. `canceled_by_client` and `defect` are now unconditional post-handoff returns and never emit `UNASSIGN_KIZ`, even when the local shipment flag arrives late. The workspace can recover an already detached order's KIZ from its withdrawal task/event history, and normal operational sync reattaches codes still present in WB metadata. Managers/admins see the same `01GTIN21serial` identification code as the CRPT export (no AIs 91/92), while viewers remain masked. `UNKNOWN` now reads «Статус в ЧЗ не указан» with a no-live-CRPT explanation. Every FBS tab has local search and relevant filters. Tests 33/33, type-check and lint passed.

2026-08-12: localized the remaining raw technical statuses in `/fbs`. KIZ compliance tasks (`OPEN`, `EXPORTED`, `CONFIRMED`, `CANCELED`), WB action statuses (`PENDING`, `RUNNING`, `SUCCEEDED`, `FAILED`) and WB action kinds now display clear Russian labels with safe Russian fallbacks for future API/schema values. KIZ circulation wording was clarified to explicit «вывод из оборота» / «возврат в оборот». Stored enum values remain unchanged.

2026-08-12: completed the Stage 1 bulk Chestny Znak workflow in `/fbs`. Withdrawal and return-to-circulation now have separate CRPT-compatible XLSX exports with one `Код маркировки` column and identification codes stripped of AIs 91/92. Every export is stored as a `KizOperationBatch`; after CRPT accepts the file, a manager can enter one document number/date and confirm the entire batch. Local `circulationState` changes only on confirmation, not on download. Direct online status verification against Chestny Znak remains unimplemented Stage 2.

2026-08-12: fixed `BUG-023` in `/fbs` analytics. WB Finance populates `deliveryMethod=FBS` mainly on zero-quantity logistics rows, while the related `Продажа`/`Возврат` rows often have an empty delivery method. FBS finance rows are now identified through `realization_reports.orderId -> fbs_orders.externalOrderId`, with direct `deliveryMethod=FBS` kept only as a fallback. The screen now shows FBS orders, cancellations, buyouts, returns, net revenue and net transfer, plus article-level revenue/transfer. Read-only DB verification for 2026-08-01 - 2026-08-11 returned non-zero results for both cabinets. The KIZ withdrawal trigger remains at WB handoff, not order creation or buyout, because remote-sale light-industry rules require submission after warehouse shipment and before actual delivery.

2026-07-25: recovered and documented `BUG-018`, a local dependency incident caused by bundled `pnpm` resolving the parent npm project from a nested Excel-report work folder and moving 38 direct dependencies into `node_modules/.ignored`. Removed only the verified report-workspace junction that caused Git/VS Code to enumerate 10,000+ apparent changes; restored dependencies with `npm ci` and regenerated Prisma Client. The dev server starts, Git has zero deleted tracked files, and read-only DB verification confirmed both cabinets and key data remain present. Prevention rule: this is an npm repository; do not run dependency-mutating pnpm commands or create `node_modules` junctions anywhere inside it, and isolate one-off report tooling outside the repository.

2026-06-28: updated `Утренний отчет WB` automation layout and writer. Removed morning-sheet `ROMI`, added `Выкупили, шт` after `Выкупили, руб`, added `ЧП на 1 ед` after `ЧП`, added `Итого с начала года` row calculated from 2026-01-01/Jan 1 of target year through target date. Workflow now checks YTD report coverage from year start, current-month advertising coverage, writes progress to `A44:C44`, and still stays DB-only with no WB API/sync calls. Live Google Sheet `Утренний отчет WB` tabs `WB Nimba` and `WB Galioni` were updated and filled through 2026-06-27; `npx tsc --noEmit --pretty false` passed.

2026-06-19: implemented dated article versions to prevent historical financial reports from mixing old and new physical products under the same WB `nmId`. Added Prisma model/table `article_versions`, `/references` tab `Версии артикулов`, server actions with non-overlap validation, and `calculateReport` version resolution by report-row date. Reports now split one `nmId` into separate rows when a selected period crosses a version change; `ArticleVersion.costPrice` overrides regular `cost_prices` only for that version/date range. Ran `npx prisma generate`, `npm run type-check`, `npx prisma migrate deploy` against local `localhost:5432/wb_cabinet`, and a DB-only Nimba report smoke check. Existing versions were not backfilled; user should manually enter known transitions such as old `Парео синяя ракушка` to new `парео синяя разводы` with exact change date/cost.

2026-06-18: fixed `BUG-015` code path for undercounted financial-report `Заказано руб.` on long periods. The report formula remains `Σ wb_orders.finishedPrice` including cancelled orders, but `REPORTS_PERIOD`/`SALES_PLAN_PERIOD` worker paths now force a full order fetch for the requested period instead of using an incremental `lastChangeDate` cursor from a potentially partial local range. `npm run type-check` passed. Concrete DB readback for `WB Nimba` and `WB Galioni`, 2026-01-01 - 2026-06-17, still needs a normal app-environment sync/readback because this shell has no `DATABASE_URL` and agents must not read `.env`.

2026-06-16: fixed advertising campaign stats overcount for combined cards (`BUG-014`). Product sync now stores WB card `imtID` as nullable BigInt `products.imtId`; `/advertising/[campaignId]` rebuilds campaign stats and nm detail from `ad_campaign_nm_stats` for the primary combined card group, keeps only meaningful nm rows (ad contact or ad orders), and normalizes basket-only order rows to order count. Added `orderSum` to ad stat tables from WB fullstats `sum_price`, so article order sum is advertising-attributed instead of all WB orders. Backfilled `imtId` for all active-account products and `orderSum` for all existing ordered ad stat periods. For `WB Galioni (WB_2)`, `Кампания от 10.06.2026`, 2026-06-10 - 2026-06-14, local backfills now produce 6 articles, 31 baskets, 8 ad orders, and 16680.00 ad order sum.

2026-06-07: fixed stock risk classification on `/stocks`. Risk now uses local DB only, combines recent non-return sales from `wb_sales` with sale quantities from `realization_reports` as a fallback, and calculates size-level risk from financial-report barcodes when available. `Нет остатка` is assigned to products with zero total sellable stock; positive stock with reasonable 14-120 day coverage is `В норме`; positive no-demand stock is `Нет продаж`; `Излишек` requires more than 120 days of coverage and at least 10 units. UI label changed from `Норма` to `В норме`, and the category filter now supports multiple selected categories.

2026-06-04: fixed `BUG-012` missing cost prices after the Finance API migration. Finance report rows can contain lowercased `vendorCode`, while references preserve product-card casing. Report reference lookups now normalize vendor-code keys before matching. For 2026-06-02 - 2026-06-03, `парео зеленое/вискоз` now shows `1478.00` cost for two units, and no sold rows in either account have zero cost. A DB-only audit through 2026-06-03 found no net-bought product without a linked cost price.

2026-06-04: completed live verification of `TASK-WB-FINANCE-REPORTS-MIGRATION`. Manual `REPORTS_PERIOD` run `bd702e2a-c703-4aad-8faf-b76d15f29b65` for `WB Galioni (WB_2)` started at 2026-06-04 16:01:59 MSK using code loaded after the endpoint migration. It succeeded with 1 attempt, 1738 report rows read, 244 new rows inserted for 2026-06-02 - 2026-06-03, `maxReportDate: 2026-06-03`, and coverage advanced through 2026-06-03. Key financial fields were non-zero where expected and comparable to prior-period rows. Future report sync results now record `sourceApi` with exact domain/method/path.

2026-06-04: implemented `TASK-WB-FINANCE-REPORTS-MIGRATION` at code level. `fetchRealizationReportPage` now calls Finance API `POST /api/finance/v1/sales-reports/detailed`, requests only fields used by `realization_reports`, paginates with `rrdId`, and normalizes renamed camelCase/string-money fields into the existing internal row mapper. Finance API throttle is now 1 request/minute. `npm run type-check` passed; `npm run lint` passed with two pre-existing `<img>` warnings. Live WB smoke verification was not run because an account and short period were not explicitly selected.

2026-06-01: documented WB Finance API migration priority. At that time report sync used deprecated `GET /api/v5/supplier/reportDetailByPeriod`; the target was `POST /api/finance/v1/sales-reports/detailed` on `finance-api.wildberries.ru`. Main implementation risks identified were Finance token scope, POST JSON request body, `rrdId` pagination, 1 req/min rate limit, `204 No data` when report is not formed, and camelCase response fields that must map into the existing `realization_reports` schema.

2026-05-26: fixed `BUG-011` scheduled automation first-run skip. BullMQ cron schedulers no longer pass `startDate` equal to the first occurrence; cron pattern with `tz: Europe/Moscow` now picks the nearest future run. Temporary scheduler test confirmed same-day near-future scheduling works.

2026-05-26: fixed `BUG-010` for `Утренний отчет WB` runtime. The workflow is now DB-only: it checks report/ad coverage and stock snapshot freshness, then writes the sheet or fails fast; it no longer calls sync services or live WB advertising APIs, and sales-plan coverage is not required. Stale `AutomationRun` `fb1825f9-a865-491f-8720-6dc951dac1e0` was marked `FAILED` after BullMQ showed no active automation job.

2026-05-26: fixed `BUG-009` next-run display for schedules. `/automations` and `/sync` now format `nextRunAt` explicitly in `Europe/Moscow`, and backend schedule helpers calculate next run/lateness from Moscow calendar parts instead of manual `+3/-3` hour shifts. `13:28` MSK now resolves to `10:28Z` and displays as `13:28`. `npm run type-check` and `npm run lint` passed.

2026-05-26: fixed `BUG-008` in `Утренний отчет WB`. Default target date now uses the Moscow calendar date, `A43:C43` progress cells are filled, duplicate monthly `orders` sync is skipped after report sync refreshed the same range, and automation results include per-account/per-step timing diagnostics. `npm run type-check` passed.

2026-05-25: implemented `/automations` and the first automation workflow `Утренний отчет WB`. Added Prisma automation settings/account/run tables, a separate BullMQ automation queue/worker/scheduler, Google Sheets service-account runtime, daily month rollover logic for `A2:P32`, and UI for spreadsheet/account/sheet mapping plus run history.

2026-05-25: corrected `Заказано руб.` calculation to include cancelled WB orders. The metric now sums all `wb_orders.finishedPrice` rows for the selected period.

2026-05-25: fixed a stuck BullMQ `REPORTS_PERIOD` run after `Missing lock for job 316. moveToDelayed`. Long report syncs no longer force full orders backfill beyond 31 days; worker lock duration was increased. The stuck Galioni run was marked failed, and the prior successful run populated `Заказано руб.`.

2026-05-25: fixed `Заказано руб.` sync gap. `reports.period` now syncs `wb_orders` for the selected period, and orders sync upserts existing rows so WB changes to `finishedPrice` / `isCancel` are reflected locally.

2026-05-25: для подготовки `Утренний отчет WB` добавлены `Заказано руб.` и `ROMI %` в финансовый отчет, `Оборачиваемость, дн.` в остатки, account-wide orders/sales sync через `sales-plan.period`, и локальный сервис `morning-report`. Google Sheet не заполнялся.

## Current Safe Next Step

Для новой development-задачи открыть `AGENTS.md`, `docs/DOCS_INDEX.md`, затем этот файл и `docs/development/DEV_CURRENT_TASKS.md`. Первоочередная миграция Finance API завершена и live-verified.

## Active Development Risks

- Financial report sync uses live-verified Finance API `sales-reports/detailed`; continue monitoring `204 No data` and exact `report.sourceApi` in future job results.
- `Заказано руб.` depends on complete local `wb_orders`; long manual report periods now force full order fetches and can be slow/rate-limited, but must not fall back to partial incremental cursors.
- Finance API may change vendor-code casing; report reference matching is normalized and must remain case-insensitive.
- Very long `reports.period` ranges can still be slow because WB Statistics API is rate-limited; prefer shorter periods for forced orders backfill.
- `reports.period` now also calls WB orders sync; respect the Statistics API rate limit and avoid wide historical ranges without confirmation.
- WB Advertising fullstats can return several combined-card (`imtID`) groups inside one campaign and zero-contact nm basket rows. Campaign detail now filters to the primary `imtID`, keeps meaningful nm rows, and normalizes zero-contact order rows; existing ordered ad stat periods were backfilled for `orderSum`, but new data still depends on normal ad stats sync.

- Не запускать production migrations и full historical sync без подтверждения.
- WB advertising live API может возвращать 429/rate limits.
- Raw report/ad tables могут быть тяжелыми без account/date filters.
- `sales-plan.period` теперь может синхронизировать orders/sales без активного плана; не запускать широкий исторический диапазон без подтверждения.
- `Утренний отчет WB` requires `GOOGLE_SERVICE_ACCOUNT_JSON_BASE64` and the target Sheet shared with the service-account email before real writes can run.
- `Утренний отчет WB` is DB-only: it must not call WB API or sync services. Missing coverage/stale stocks should fail fast and be fixed via separate sync jobs.
- Stock turnover/risk depends on recent local sales coverage. If both `wb_sales` and `realization_reports` are stale for the 30 completed days before the stock snapshot, `/stocks` can still show many `Нет продаж` rows.
- Automation worker/scheduler are separate from sync worker; production enablement requires Redis/PostgreSQL and explicit rollout setup.
- Старые flat `docs/*.md` теперь legacy redirects/archives.

## FBS Implementation Handoff (2026-07-30)

- `/fbs` is a separate workplace for seller warehouse stock, FBS assortment, orders, supplies/stickers, KIZ and Chestny Znak queue.
- Migration: `prisma/migrations/20260730120000_fbs_operations/migration.sql`. It was schema-validated and applied to the local development `wb_cabinet` database on 2026-07-30 after BUG-019. It has not been applied to production.
- FBS schedules: `fbs.operational` 5 min, `fbs.stocks.current` 15 min, `fbs.marking-report` 60 min. Defaults are disabled; scheduler was not applied.
- Initial backfill script is fixed to `2026-07-20` - `2026-07-30`, dry-run by default and was not executed.
- Full KIZ is encrypted; do not log `rawCode`, decrypted codes or finance KIZ. Use masks/hash.
- All WB FBS writes require manager intent, a warehouse gate and `FbsActionLog`; only admin enables a gate. All gates default false.
- Stage 2 True API is not implemented.
- BUG-020 live-read verification passed for one local cabinet: 1 warehouse, 4 supplies, 12 one-day orders, 30 marking rows, 7 distinctly marked current positions, 16 positive/current FBS positions and 215 WB stock units. No WB write was made.
- BUG-021 fixed raw FBS statuses and the overflowing assortment selector. All documented seller/WB statuses and order actions now have Russian labels; the catalog popover has search, collision padding, a 60vh/20rem height cap and internal scrolling.
- Local/WB stock labels and the WB write gate were clarified in the interface. Permission alone sends nothing; publication remains a separate explicit action.
- Official and live verification confirmed that FBS metadata already provides `orderId -> meta.sgtin.value[]` after the code is attached in WB. Across both local cabinets, 20 of 24 latest orders returned a non-empty SGTIN array; no raw code was printed or stored during verification.
- Secure WB-metadata ingestion is implemented: SGTIN is extracted before sanitization, normalized/parsed, encrypted, hash-deduplicated, GTIN-validated and linked to its order; stored order metadata remains redacted. Conflicts are counted and flagged without exposing or moving the code.
- Two-cabinet live verification for 2026-07-29 - 2026-07-30 created and assigned 20 KIZ units (11 Galioni, 9 Nimba), with 0 rejects, conflicts or GTIN mismatches. The idempotency rerun created/assigned 0 and recognized all 20 as already assigned.
- All 20 imported units reconciled to `HANDED_OVER` and received open `WITHDRAWAL_REMOTE_SALE` tasks. This local DB mutation was expected; no WB write occurred. PDF import is now optional for pre-packing code-pool accounting.
- BUG-022: 9 Nimba codes from 2026-07-28 were missing because two running sync workers had loaded pre-ingestion code. After a bounded direct repair, both stale worker trees were replaced by one current worker. A queued idempotency check succeeded with all 9 already assigned.
- BUG-024: `canceled_by_client` and `defect` preserve their KIZ association. Historical display can recover from task/event relations; if neither exists and WB no longer returns metadata, NimbaOS cannot invent the old code and a normal operational sync is the only safe recovery attempt.
- Authorized managers/admins see CRPT identification codes `01GTIN21serial` in FBS tables and selectors; viewer access remains masked. Never log or expose decrypted AIs 91/92.
- `UNKNOWN` is a truthful local absence of a confirmed Chestny Znak state, displayed as «Статус в ЧЗ не указан»; do not silently convert it to `IN_CIRCULATION`.
- WB metadata readiness no longer depends on Chestny Znak circulation state. July 28 workspace readback is 9/9 KIZ with label `Получены`; exact safe reasons replace the former generic `Заблокировано`.
- `BUG-023` fixed FBS analytics: financial sale/return rows are matched to FBS orders by `orderId`, because WB puts `deliveryMethod=FBS` mostly on related logistics rows. Orders/cancellations use FBS order date; buyouts/returns/revenue/transfer use the financial operation date.
- Remote-sale withdrawal tasks intentionally remain tied to WB handoff (`supplierStatus=complete`), not initial order creation or final `sold`: light-industry distance-sale reporting is due after shipment, within 3 business days and no later than actual delivery. A returned unit is quarantined and uses return-to-circulation/remarking as applicable.
- Verification completed: Prisma format/validate/generate, 14 FBS tests, TypeScript and lint. Authenticated visual verification of BUG-021 remains pending because the test browser had no active application session. Existing unrelated `<img>` warnings remain.

## Mini-PC Production Handoff (2026-08-21)

- Initial historical synchronization for the new `WB AGNIA` account was explicitly authorized and queued for `2026-01-01` through `2026-08-20`: cards, reports/storage, advertising campaigns/statistics, current WB stocks, reviews and questions. Cards completed with 20 created products and zero errors; campaigns completed with 6 rows and zero errors. Advertising stats is processing first, and the remaining jobs are queued behind it. Do not duplicate these active historical jobs.
- Production checkout: `C:\NimbaOS\nimba`, branch `main`, deployed commit `abb5853ddbb0636a98ac86e8f853081d64665b05`.
- Controlled release path is operational. CI runs automatically on pushes/PRs; production changes only after the owner starts the manual `Deploy production` workflow and checks its confirmation box. The mini-PC self-hosted runner is an interactive highest-privilege scheduled task under `n8929` because Docker Desktop needs that logged-in Windows session.
- Successful release workflow: GitHub Actions run `32679512118`. It built `nimba-app:abb5853ddbb0`, backed up PostgreSQL, found/applied committed migrations as needed, recreated app/sync worker/automation worker, re-applied both scheduler sets and passed local plus Tailscale health checks.
- Current deployment backup: `C:\NimbaOS\backups\nimba-production-20260824-012722-abb5853ddbb0.dump`; size 9,891,769 bytes; SHA256 `ADB8664F1A24EA97394EE02823FF3FC9CC778B9D22CBAFD1924A644130F836C2`; archive validation passed. Last-successful deployment metadata is stored at `C:\ProgramData\NimbaOS\deployments\last-successful.json`.
- The release script changes code/images and applies committed migrations; it does not replace the PostgreSQL/Redis volumes. Automatic failure recovery rolls application services back to the prior image, but never restores a database dump without separate explicit authorization.
- Pre-rollout backup: `C:\NimbaOS\backups\nimba_production-20260821-005218.backup`; SHA256 `967410A71AE53435CC1E727A29898807B942DAD36043D614EBA3EA6D9AF1AB50`; archive list validation passed.
- Production build completed for `nimba-app:latest` and `nimba-worker:latest`. App and worker were recreated; PostgreSQL and Redis were not recreated.
- Prisma reports 13 repository migrations with no pending work. `20260730120000_fbs_operations` is finished in production; one older `user_preferences` attempt is explicitly rolled back and followed by a successful record.
- Verification: app/PostgreSQL/Redis healthy, sync worker started, `/api/health` HTTP 200, and basic retained-data counts are unchanged.
- Remote administration: Tailscale/OpenSSH works at `n8929@100.107.244.75` using the trusted laptop key. AnyDesk is the graphical fallback.
- Trusted-laptop task `NimbaOS Temporary Tunnel` exposes the private application only at `http://127.0.0.1:13000`; it starts at laptop logon and restarts after SSH failure. Tailscale must be connected on both devices.
- The mini-PC's 2026-08-16 Windows Update reboot explains the earlier login screen and stopped application: Docker Desktop requires the `n8929` interactive session. Session lock is safe; reboot or sign-out requires a new Windows login before Docker Desktop can run.
- All saved mini-PC Wi-Fi profiles are automatic. `Keenetic-7780` is first in profile order, but Windows currently uses `MTS_GPON5_FCEA`, likely after Keenetic was unavailable during reconnect.
- Public `app.nimbaos.ru` currently points to the native Cloudflare Tunnel, but must not be treated as production-ready for Russian IPv4 clients: large Next.js files are truncated before hydration. The obsolete Worker custom domain was removed. Trusted Tailscale devices can use `https://win-sk69nvld6f0.tailc11887.ts.net`; the durable no-VPS public fix is direct HTTPS over a public/static IPv4 with Cloudflare proxying disabled.
- FBS schedules, automation workers/schedulers, bounded backfill and WB write gates were not enabled during this rollout.

## Read Next If Needed

- `docs/core/ARCHITECTURE.md`
- `docs/core/DATA_MODEL.md`
- `docs/core/WB_API_MAP.md`
- `docs/core/DATABASE_ACCESS_GUIDE.md`
- `docs/development/BUGS_AND_INCIDENTS.md`
- `DECISIONS.md`

## Do Not Do

- Не читать `.env`.
- Не менять WB prices/cards/ads/feedback answers без подтверждения.
- Не запускать migrations/sync worker/scheduler без причины и разрешения.
- Не читать все docs подряд.

## Last Updated

2026-09-01 - FBS movement-sheet workflow now maintains a separate current `Остатки WB` snapshot (stable account/nmId/chrtId upsert), verifies Summary totals per product/account against NimbaOS, and treats a missing local replenishment as a non-blocking warning. Live Sheet totals were verified at Nimba 264 and Galioni 278; retry was fully idempotent. The local setting is enabled/applied for 23:30 MSK; migration/code are not deployed to production.

2026-08-24 - Corrected the public-ingress diagnosis: Russian IPv4 clients receive only the first roughly 16-24 KB of larger Cloudflare responses. Removed the Worker custom domain, restored Tunnel DNS and documented Tailscale as the current reliable path plus public/static IPv4 with direct HTTPS as the no-VPS public solution.
2026-08-24 - Interim Worker hydration mitigation was tested but did not make the public path reliable and was superseded by the corrected diagnosis above.
2026-08-23 - Public ingress restored through Cloudflare Worker and Tailscale Funnel; immutable Next.js assets use the Worker's technical hostname to avoid stalled large responses on the custom domain.
2026-08-21 - `WB AGNIA` initial historical synchronization was authorized and queued for 2026-01-01 through 2026-08-20; cards and campaign metadata completed successfully, remaining jobs are processing sequentially.
2026-08-21 - current `main` deployed to mini-PC; production backup, migrations, app/worker images, health and Tailscale/OpenSSH access verified. Public Cloudflare ingress remains blocked.
2026-08-14 - mini-PC production runtime verified locally; public Cloudflare ingress remains blocked by an unstable provider/network path and confirmed double NAT/CGNAT.
2026-08-12 - fixed and DB-verified FBS analytics under BUG-023; documented the legally required handoff-time KIZ withdrawal trigger. No sync, WB write, migration or production-data mutation was run.
2026-08-13: rebuilt the two affected historical Galioni CRPT withdrawal files after canceled-order reconciliation. The 2026-07-30 batch was reduced from 16 to 11 rows and the 2026-08-12 batch from 179 to 173 rows; 5 and 6 canceled tasks were excluded respectively. The corrected workbooks contain 184 unique KIZs total, all still `EXPORTED + WITHDRAWAL_REQUIRED`, preserve the verified WB unit prices, and have no overlap. Originals remain unchanged.
